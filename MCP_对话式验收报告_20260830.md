# Lvke MCP 当前未关闭问题

| 项 | 值 |
|---|---|
| 对照日期 | 2026-09-01 10:30（源码实测复核 + 全量测试） |
| 上一轮 HEAD | `9edfaa8` |
| 全量测试 | `670 passed` / `1231 subtests`（基线 `658`，本轮净增 12 条行为测试） |

上一轮记录的 C-1..C-5 五项已逐条实测确认成立，并已全部修复。下表只保留仍需人工动作的项。

## 仍需人工动作

| 编号 | 类 | 问题 | 动作 |
|---|---|---|---|
| C-2 | 构建资格 | `build_metadata_complete=false`：`build_time` 在「commit ≠ HEAD」或「tracked 工作树 dirty」时被清空 | 提交本轮改动后执行 `python scripts/write_build_metadata.py`，再重启 MCP 宿主。顺序不能反：带未提交改动时写出的 `build_time` 会立即被判 `tracked_worktree_dirty` 而清空 |
| C-5 | 环境 | 配置已修，但**需重启宿主才生效**：当前进程仍持有旧 env | 重启 Claude Code / Cursor 后用 `source_import_local_path` 复验 |

## 本轮修复（均带回归测试）

| 编号 | 严重度 | 问题 | 修法 |
|---|---|---|---|
| C-1 | P1 | 资金缺口检查读错字段：引擎真生产者 `annual._build_financial_plan` 写 `gap`，`checks.py` 读 `funding_gap`，导致「财务计划无资金缺口年」恒 `ok=true` | `checks.py:77` 改读 `gap`。实测复现：注入真缺口年原报「缺口年数 0/2 ok=true」，现报「1/2 ok=false」；同时钉住无缺口年不得误判 |
| C-1b | 根因 | `statements.financial_plan_rows` 是同语义第二份实现且零调用方。该死代码已两次害人：附表11 列定义照它写导致 5 列恒 `None`（前修）、`checks.py` 照它读键导致缺口检查失效（本轮） | **删除该函数**，而非只改调用点——留着它下一个消费方还会读错键。刷新 `api_snapshot.json` 基线（语义 diff 已核对：仅此一个符号消失） |
| C-3 | P2 | 审查标准仍写「十三张财务附表」，交付引擎已 14 张 | 标题改「十四张财务交付附表」，描述说明附表11 并入。**`requirement_id` 保持 `REQ-FINANCE-13-TABLES` 不变**——它同时是 `sim_a_templates/generic/*.md.j2` 的文件名解析键与已固化对象的引用键，改 ID 会断链；已在描述里注明该字样不代表张数。同步修 `.md.j2` 正文 |
| C-4 | P2 | 模板目录缺附表11。实测范围比原记录更大：`TEMPLATES` 19 项无 `financial-plan`、`delivery_appendix_list()` 只返回 13 条、`table_pack` markdown 标题写死「13 张」 | 补 `financial-plan` 模板（列键与 `_table_render/specs.py` 逐项一致，不另造一套）、补 `_APPENDIX_META` 附表11、补 `_STANDARD_APPENDIX_DICT`（`vendor_sheet=None`，甲方底稿实测无此表）。markdown 标题改为从 `len(_DELIVERY_META)` 派生，不再写死 |
| C-5 | 配置 | `LVKE_SOURCE_IMPORT_ROOTS` 指向不存在的 `~/Desktop/lvke-资料`，且该变量会**覆盖**本可用的 corpus manifest | 从 `~/.claude.json` 与 `~/.cursor/mcp.json` 移除该过期变量（两处都中，原记录只提了 cursor 一侧），回落到已正确配置的 `LVKE_EXTERNAL_CORPUS_ROOT=<repo>/docs`，实测解析出 4 个真实资料根。两文件均已备份 `.bak-20260901100639`，并校验 14 个 lvke server、8 个第三方条目、`*_FILE` 密钥引用全部无损 |

## 复核中发现的既有偏差（未修，非本轮引入）

`table_pack.DELIVERY_TABLE_KEYS` 与模板目录的 `template_id` 命名不一致：引擎用
`investment` / `funding`，目录用 `investment-estimation` / `financing`。该差异在
上一轮 HEAD 已存在，不影响交付（两侧各自自洽、张数已对齐），但跨侧按 id 直接
join 会漏配。需要统一时应单独处理，避免与本轮修复混在一起。
