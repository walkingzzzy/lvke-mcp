# Lvke MCP 当前未关闭问题

| 项 | 值 |
|---|---|
| 对照日期 | 2026-09-03（第八轮活体验收 + **R8-1/R8-2 修复后重启复测**） |
| HEAD / 活体信封 | 修复提交 `e193c4d6aec61e82329c0831eda7d66d2d4664a7`。第八轮全量链仍记在 `4c95d69` |
| 构建元数据 | 复测信封 `build_commit=e193c4d`，`build_metadata_complete=false`（缺 `build_time`；工作区有未提交报告/能力介绍，不是产品回归） |
| plugin_version | `0.1.0+codex.20260902102519` |
| 实时工具分母 | **203**（第八轮重启后 `tools/list`；本复测未重数全量） |
| 本轮工作区 | 轨 A `ws20260903r8`；零材料轨 B `ws20260903r8z`；sidecar `ws20260903r8s`。P1 复测 `ws20260903r8p1`。禁止复用 `ws20260903r7` / r6 / r5 / r4 / acc |
| 项目 | 江夏智慧农业产业园新建项目（公开资料 `evidence_track=real`，`project_fact_certified=false`） |

## 当前状态

- **第七轮 G1 元数据缺口本轮已关。** 信封 `build_metadata_complete=true`，不再缺 `build_time`。这只过构建门，不替代正式资格。
- **轨 A 主链走到过程验收成功终态 `process_accepted`。** Release `fdrp_e903f2adfdae4784d71af6b4`，FDR `fdr_6f664a40835b301bc66dd586`，`release_scope=process_acceptance`。`project_delivery` 继续硬拒。
- **轨 B 独立零材料链已走完 TemplatePack，晋升被正确挡住。** 拟定包 `zmtp_4dbe9ade93cebaca1e91ea60` 已生成；`delivery_confirm_formal_promotion` 因技术/七域验收未过返回 `formal_promotion_acceptance_required`。未把 `zmr_*` 原地升级。
- **真实资料轨仍为 `partial`。** EvidencePack `evp_25640d2a04a26a344f1422f6` 的 `formal_evidence_candidate=false`，`project_fact_certified=false`。公开网页不得升成 `formal_evidence`。
- 多数业务调用仍是 `diagnostic_only=true`、`formal_report_allowed=false`。
- **R8-1 / R8-2 已在 `e193c4d` 修复后重启复测关闭。** 旧 `ws20260903r8*` 对象不作关项证据。
- F-9 / F-15 按既有口径保持，不改成禁止出表。

> 在本轮实时工具、场景和边界覆盖范围内，技术金标链可计算到一致 FinanceRun / 十四表 / 真九章修订 / 审查 / 过程验收。
> 真实资料资格、正式候选与 release 条件分别判定，不相互替代。
> **本地 P1 已关。** 这不是正式候选或 release 通过。

## 本轮仍开项

| 编号 | 级别 | 结论 | 本轮证据 |
|---|---|---|---|
| **F-9** | 既有 | 流资分项净额 ≠ 申报/估算流资时必须进 `blocking_issues`，查询侧 `business_success=false` | 轨 A：`run_44f43583f553` 871.11 ≠ 1500，工程量 20780 ≠ 23800。轨 B：`run_3e61cde8e3d9` 分项净额 **6770** ≠ 估算 **9545**（差额 -2775） |
| **F-15** | 降级设计 | 不一致/非正式 run 仍可出表，但不得绑定正式报告 | 轨 A `tables_validate(formal)` → `finance_reference_structure_incomplete`。轨 B 表包 `usable=false` / `release_grade=unavailable` |

### 残余观察（不升格）

| 观察 | 不升格原因 | 本轮证据 |
|---|---|---|
| 资产负债表权益残差 | 按契约披露，不静默补平 | `fbs_418437d014fbdcdf7800a306`：`balance_ok=true`，`equity_reconciliation_ok=false`，mismatch 期 2–10，建设期末残差 **-871.11**（等于流资） |
| `finance_promote_to_formal` | 预览 run 不得晋升；失败码偏糊 | `prior_run_id=run_74d03d2aa69b` → `run_creation_failed`，「创建新 run 失败」，`blockers=[]` |
| 零材料行业解析 | 活体真值，不是解析崩溃 | 一句话含「产业园」→ `park_infrastructure` + 工厂场景落到 `construction_real_estate.residential.base`，不是农业 |
| 知识治理达不到 passing | 门禁正确 | `review_score_section` 财务章 `weighted_score=2.8864`、`passing=false`（`section_citation_missing`）。`knowledge_submit_candidate` → `knowledge_candidate_rubric_not_passing` |
| `dr_continue` 未提交即续研 | 门禁正确 | `task_not_continuable`：「Agent 研究尚未提交；请先完成 dr_submit」 |
| 过程验收质量提示 | `quality_valid=false` 不阻止 `process_acceptance` | 审查 122 阻断、P0=11、`verdict` 不完整写入 limitations，未进 blockers |

## 本轮已关闭（不要再当缺口）

| 编号 | 结论 | 决定性证据 |
|---|---|---|
| **R8-1** | **修复后关闭** | 重启后 `tools/list` 的 `review_disposition_finding.properties` 含 `disposition` 全枚举。新工作区 `ws20260903r8p1` 调 `disposition=confirmed` + `note` → `transport_success=true` / `system_success=true` / `outcome=blocked` / `code=review_not_found`（业务 blocked 允许），**不再 MCP `-32602`**。`trace_id=mcp_89316064ad6948f8bd3ae252bf4d1d28`，`build_commit=e193c4d` |
| **R8-2** | **修复后关闭** | 同一份不完整光伏 spec：`status=partial`，`field_errors`/`warnings`/`quality_issues` 均为非空字符串诊断，`code=null` ≠ `invalid_tool_output`，`transport_success=true`。`trace_id=mcp_be53d9b479494493a55cd9681e0d3d92`，`spec_hash=sha256:a28600f74d3f500887f4e08038cf667144ca201f3db9d16c63122b80cec5613f` |
| **G1 构建元数据** | **第八轮关闭** | 第八轮全量链信封 `build_metadata_complete=true`，`build_time=2026-09-03T01:03:22Z`。本复测因工作区脏而缺 `build_time`，不重开 G1 |
| **F-1 过程验收** | 再关 | `process_acceptance` → `status=process_accepted`，`fdrp_e903f2adfdae4784d71af6b4` / `fdr_6f664a40835b301bc66dd586` |
| **F-9** | 再关，未回潮 | 不一致 run 顶层 blocked；对齐后主链 `run_74d03d2aa69b` `consistency_ok` 由 `finance_get_run` 给出 `status=ok` / `quality_status=pass` |
| **零材料 TemplatePack** | 本轮补测关闭「未触达」 | `delivery_generate_template_pack` → `zmtp_4dbe9ade93cebaca1e91ea60`，141 行映射，披露标记已写入。`query_success` 未当交付状态；`usable_artifact_count=0` |
| **DR 恢复链** | 本轮补测 | `dr_prepare` → `dr_start(drs_5f51c8c8e684c7398b97dd54)` → `dr_create_checkpoint(drcp_21f2d5a34387781379a36ecd)` → `dr_resume(drs_ab7f9795cfa9d3868fec3b1f)` → `dr_cancel`。原任务未取消 |
| **finance 扩展** | 本轮补测 | `finance_build_balance_sheet` / `finance_run_monte_carlo` / `finance_generate_package`(deprecated, missing_inputs) / `finance_promote_to_formal`(blocked) |
| **source-files 小文件** | 本轮补测 | `src_0528b9cf6754aa485eec6aa1`，93 B ASCII，预览原文正确，`formal_evidence_candidate=false` |
| **reference / geo / template_fill** | 本轮补测 | 江夏区 geocode `30.349, 114.321`；`template_fill(key-indicators)` 按行键写入 25571.11 / 871.11 |
| **Tavily extract / map / crawl** | 本轮补测 | 武汉市政府首页可抽取；map 8 URL；crawl 3 页 |

第七轮已关且本轮未回潮：动态回收期假数字、`run_model` 信封、`next_actions` schema、parent binding、Skill 14 表口径、F-2 / F-3 / D-2 / N-1 / F-4 / F-5 / F-7 / F-8。第四轮及更早已关：F-6 / F-10 / F-12 / F-13 / F-14 / D-1 / D-5。C 系列仍关。

## 本轮挡住的攻击（EXPECTED_REJECTION）

| 类别 | 场景 | 结果 |
|---|---|---|
| 正式资格 | 轨 A `project_delivery` | `FORMAL_ARTIFACT_QUALIFICATION_REQUIRED` + `evidence_policy_mismatch`（drp / fsp / review / rrv） |
| 正式资格 | 轨 A `report_export_docx(kind=formal_candidate)` | `FORMAL_ARTIFACT_QUALIFICATION_REQUIRED` |
| 正式资格 | 轨 A `tables_validate(formal)` | `finance_reference_structure_incomplete`（F-15 保持降级出表） |
| 正式资格 | 轨 B 未晋升 `report_export_docx(formal_candidate)` | `FORMAL_ARTIFACT_QUALIFICATION_REQUIRED`（`rrv_6cfdbe0ed14380c0b9aff102`） |
| 正式资格 | 轨 B `feasibility_release(project_delivery)` | `controlled_assumption_formal_forbidden` + `preview_cannot_formal_release` |
| 正式资格 | 轨 B `delivery_confirm_formal_promotion` | `formal_promotion_acceptance_required`；acceptance.technical.blockers 含 `finance_run_consistency_failed` |
| 勾稽 | 轨 A 未对齐 run / 轨 B 预览 run | F-9 进 `blocking_issues`；工件 `usable=false` |
| 收购预览 | 不完整光伏 spec 跑模型 | `SPEC_VALIDATION_FAILED`（缺利用小时/剩余年限等） |
| 收购绑定 | `report_prepare(finance_binding.kind=asset_acquisition)` 绑通用 run | `draft_ready=true` 但 `formal_ready=false`，blockers=`finance_run_required` + `acquisition_tables_package_required` |
| 知识治理 | 未达标 rubric / 不存在候选 | `knowledge_candidate_rubric_not_passing`；snapshot/review/publish → `knowledge_candidate_not_found` |
| 零材料工件 | `delivery_get_artifacts` | `query_success=true` 但 `domain_status=partial`，`usable_artifact_count=0`，`formal_report_allowed=false` |

过程验收本身**放行**：技术校验质量问题进入 `release_limitations`，不阻止 `process_accepted`。这是过程验收语义，不是 F-1 失效。

## 轨 A 主链真值（只引用 MCP，不重算 IRR/NPV）

对齐 Run **`run_74d03d2aa69b`**（`status=ok`，`quality_status=pass`）：

| 指标 | 值 |
|---|---|
| 总投资 | **25571.11** 万元 |
| 建设投资 | 23800 |
| 建设期利息 | （含在总投资口径） |
| 流动资金 | **871.11** |
| 工程量 | 19800（单价口径对齐后） |
| 达产收入 | **6720** |
| 经营成本 | 4928.75 |
| IRR | **4.87%** |
| NPV | **-3941.06** |
| 静态回收期 | **8.42** 年 |
| 动态回收期 | **`None`（正文写 —）** |

规划合计「26200」不得写入财务叙事。去掉后 `finance_narrative.ok=true`。

Monte Carlo（seed=20260903，收入三角 0.9/1/1.1，n=50）：IRR P5/P50/P95 = **3.498 / 4.825 / 6.222**；NPV P5/P50/P95 = **-5590.898 / -3992.145 / -2264.362**。样本未落盘。

十四表：`ftp_cd92c34dc64957f15fa2c842`（14/14，`bindable_to_report=true`，`formal_report_allowed=false`）。CSV `ftc_3650a418bb875250bdd0073b`（含 `00-lineage`）。XLSX `sha256:b4dfebf052a3298648d6efa59de1aefae27265c253fa66534be0a73b62f8e99d`。

真九章终修订 **`rrv_92568c92f9bb5b1b475c0b63`**。标题为：概述、背景必要性、需求与产出、选址要素、建设方案、运营、投融资财务、影响、风险、结论。草稿 DOCX `deliverable_eb16401b404b4c8696d84f57ade9ff4b`，CJK `invalid_locale_font_count=0`。

审查 `review_ca6877bee81f4d748f9078836fca8549`：122 阻断，P0=11，未假合格 finalize。导出 `rvexp_a7ec36e17d2c603ffc431e22`（json + markdown）。

## 轨 B 零材料（独立对象，禁止与轨 A 混用）

| 对象 | ID |
|---|---|
| Intent | `zmi_88fb3811b1698cc73410739f` |
| 确认后 Run | **`zmr_eac571488afc2ef17517c088`** |
| 确认后假设包 | `zma_4a15a4fcb7f6856fddc22486` |
| 预览 FinanceRun | `run_3e61cde8e3d9`（`consistency_ok=false`） |
| 表包 | `ftp_189a7002225f2d18dee61980` |
| 拟定模板包 | **`zmtp_4dbe9ade93cebaca1e91ea60`** |
| 预览修订 | `rrv_6cfdbe0ed14380c0b9aff102` |

已确认种子（均为 `controlled_assumption`）：年收入 60000、总投资种子 98634.19、贷款比 0.45、工期 24 月、利率 0.042、运营 8 年。利率 0.042 不是禁写的假动态回收期 4.2 年。

引擎预览数（**不得当合格正式指标**）：总投资 101462.64，达产收入约 59999.99，IRR 12.54%，NPV 12690.35，静态 3.86，动态 4.59。房地产去化：开发成本按存货，不折旧。

`delivery_transition`：取消初始 `zmr_63297fe3a1ac924ad6766afa` → 新取消快照 `zmr_26eee2c6f17311c73f2c71f2`；resume → `zmr_c50a91f4ab3538fee669274a`。主链 `zmr_eac571488afc2ef17517c088` 未取消。

## 本轮物理核验（不采信工具自述）

| 对象 | 核验方式 | 结果 |
|---|---|---|
| 对齐 Run 指标 | `finance_get_run` 只引用 | 25571.11 / 6720 / 4.87% / -3941.06 / 8.42 / 动态 None |
| 轨 B 流资勾稽 | `finance_get_run(run_3e61cde8e3d9)` | 6770 vs 9545，`consistency_ok=false` |
| 零材料工件 | `delivery_get_artifacts` | 20 个交付类工件全部 `usable=false`；`query_success` 不是交付状态 |
| sidecar 小文件 | `source_file_get` 预览 | 原文 `# sidecar fixture ws20260903r8s` 正确，未升正式证据 |
| 模板填充 | `template_fill` 二次调用 | 总投资/流资两行写入；错键会被丢弃并警告 |
| 收购硬拒 | `acquisition_run_model` | `SPEC_VALIDATION_FAILED` 列出缺项 |

**交付表张数仍是 14 张，不是 13 张。** `engine_delivery_count=14`。

不要用第七轮数字当本轮真值。第七轮对齐是 25549.56 / IRR 5.2% / NPV -3072.39；本轮是 **25571.11 / 4.87% / -3941.06**。

## 两轨结论

| 轨 | 结论 |
|---|---|
| 技术金标 | 主链可算、一致 run 可出十四表、真九章可固化、审查可导出、过程验收拿到 `process_accepted`。**R8-1 / R8-2 已关。** 未重跑全量主链，正式验收仍按第八轮对象分别判定。 |
| 真实资料 | `partial`。公开来源已固化但 `formal_evidence_candidate=false`；`project_fact_certified=false`。 |
| 正式候选 | **不可用。** 未做 EVD-2 晋升；审查 P0 未关闭；`formal_report_allowed=false`。 |
| 零材料 | 拟定模板包可生成；未晋升正式导出/发布/晋升均硬拒。晋升成功后必须**新建**对象链，禁止升级 `zmr_*`。 |
| release | `process_acceptance` **成功**。`project_delivery` 继续硬拒。 |

## D 系列

| 编号 | 本轮是否复验 | 结论 |
|---|---|---|
| D-1 | 更早已关 | 关闭 |
| D-2 | 轨 A 草稿 DOCX 已出；本轮未再注入四假数字 | 第七轮关闭口径保持；本轮未重复造假实验 |
| D-3 / D-4 | 收购 sidecar：不完整 spec 保存为 candidate、跑模型硬拒；`validate_spec` 现走 `partial` 诊断 envelope | **R8-2 已关**；完整光伏收购全链未跑 |
| D-5 | 并入 F-1 | 已关 |

## 本轮未触达（如实，不是 SKIPPED）

可以安全执行但本轮范围未及，不得写成已测：

- 完整光伏/酒店收购确认后出表与 formal 工件（D-3/D-4 全链）
- 零材料晋升成功后的**新对象链**重建套件（晋升被验收门挡住，属预期）
- `review_retest` / dimension 确认 / package confirm / `review_finalize`（finalize 禁止假合格）
- 知识治理 snapshot→accepted→publish 成功路径（无 passing 候选）
- 假阳性三组（披露句 / 敏感性三情景 / 项目 IRR vs 资本金 IRR）本轮正文未写
- 分散四假数字单独立修订
- SSRF / 云 metadata 本轮未复打

`mcp_auth` 不当业务工具。上表是明确未触达，不是「其余都过了」。

## 修复建议顺序

1. F-15：降级出表可保留，继续保持「非正式候选 / 不得绑定报告」。
2. 零材料预览流资 6770≠9545：假设包没有 WC 字段可确认；若要对齐需引擎或假设字段补 `working_capital_wan`，不要静默改种子。
3. 正式候选仍缺 EVD-2 / 项目事实认证 / 审查关闭；不要用过程验收成功替代。
4. 要关审查 P0，须在有真实 `review_id`/`finding_id` 的工作区继续 `review_disposition_finding`；本复测只证明传输可达。

## 本轮对象血缘（只使用这些 ID）

### 轨 A `ws20260903r8`

```text
pctx_72a6033b3e2bda5f630dd15e
src_d2ae43c9286cb871f362b103   正文 sha256:8a712137… / 对象 sha256:d2ae43c9…
evp_25640d2a04a26a344f1422f6   formal_evidence_candidate=false
drp_0fc1f9f8aefe56a7daa024fc   accepted_with_limitations
mkt_e689db4620908346509f172f   22400000×0.0003=6720
optcmp_e09b0e6be557d5d51a24126b   选 planA，密度 0.32
scale_89d6f88846fba43e73f6ef19   8万㎡
revdrv_34f20beeef20b37a7aa0fb90   爬坡 4032/5712/6720，price_unit=wan
labor_bf12e5090105eb652ce73f92   59人，工资 456.48，福利 159.77
costdrv_e6fbc51b316ec1babc97c4ef   规划总投资 26200
ffp_fd1b2b8d5555a2d61db992d4   Fact Pack，source_reconstructed
fsp_70e5adb8dbfa52043fdf03d1
fboe_2c12e98843ba32cb5b1d1795
run_44f43583f553   F-9 不一致（废弃，不得当主链）
run_74d03d2aa69b   对齐主链
ftp_cd92c34dc64957f15fa2c842
ftc_3650a418bb875250bdd0073b
rprep_5181057dbba65c4f63da4366
rrv_92568c92f9bb5b1b475c0b63   终修订（父 rrv_076e5feba266ebe9fae5325c）
deliverable_eb16401b404b4c8696d84f57ade9ff4b
review_ca6877bee81f4d748f9078836fca8549
rvexp_a7ec36e17d2c603ffc431e22
fdrp_e903f2adfdae4784d71af6b4 / fdr_6f664a40835b301bc66dd586
fbs_418437d014fbdcdf7800a306
fmc_97a47b629b12dc1380dda1cb
rva_cb1ca2fa2235e057525a37d8 / rva_b6ebce4f2978d61e51182b20
```

输出目录：`lvke产出/ws20260903r8/report/deliverable_artifacts/artifacts/deliverable_eb16401b404b4c8696d84f57ade9ff4b`。

活体顺序：**先 `finance_confirm_spec`，再 `finance_build_basis_of_estimate`**（与部分 Skill 叙述相反，以活体为准）。`report_prepare` 不要同时传 `finance_binding` 和顶层 `run_id`/`finance_tables_package_id`。`finance_run_model` 不能同时传 `spec_id` 和 `spec`。`review_list_findings` 用 `severity=P0`。

### 轨 B `ws20260903r8z`

```text
zmi_88fb3811b1698cc73410739f
zmr_63297fe3a1ac924ad6766afa   初始（后被 sidecar cancel）
zmr_1be03a27f7f63887bc24dcf7   start 后未确认
zma_f8f43cd2576c75c34b399a6c   未确认假设
zma_4a15a4fcb7f6856fddc22486   确认后新包
zmr_eac571488afc2ef17517c088   确认后主链
run_3e61cde8e3d9
ftp_189a7002225f2d18dee61980
zmtp_4dbe9ade93cebaca1e91ea60
rrv_6cfdbe0ed14380c0b9aff102
fdr_70a27dddfae7446060285f1e   预览 FDR（project_delivery 硬拒）
zmr_26eee2c6f17311c73f2c71f2   cancel 快照
zmr_c50a91f4ab3538fee669274a   resume 新 Run
```

### sidecar `ws20260903r8s`

```text
src_0528b9cf6754aa485eec6aa1
drs_5f51c8c8e684c7398b97dd54
drcp_21f2d5a34387781379a36ecd
drs_ab7f9795cfa9d3868fec3b1f   resume 后已 cancel
spec_3825e30bf3a848df8bcfcff76ffce229   不完整光伏候选
```

### P1 复测 `ws20260903r8p1`（关 R8-1/R8-2 只用这里）

```text
pctx_0b8b23d51dd4c63b4571cc1e
review_disposition_finding  trace_id=mcp_89316064ad6948f8bd3ae252bf4d1d28
acquisition_validate_spec   trace_id=mcp_be53d9b479494493a55cd9681e0d3d92
```

旧 `ws20260903r8*` 对象不作本复测关项证据。

## 方法笔记

第八轮全量链只使用实时 MCP，未用 pytest / 脚本 / 历史对象顶替。R8-1/R8-2 在 `e193c4d` 改 `src/` 后冻结提交、重启一次，用新工作区复测。未把公开网页升成 `formal_evidence`。未注入 35000 / 12000 / 18.60% / 4.2。数字只引用本轮 MCP 真值。

分类只用 `PASS` / `EXPECTED_REJECTION` / `UPSTREAM_FAILURE` / `SKIPPED`。门禁拒绝计 EXPECTED_REJECTION，不写成功能坏。`query_success` 不是交付状态。
