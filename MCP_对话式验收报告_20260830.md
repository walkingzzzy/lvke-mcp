# Lvke MCP 与 Skills 对话式活体验收报告

| 项 | 值 |
|---|---|
| 验收日期 | 2026-08-30 20:16 – 22:35（首轮 + 修复复验）；**2026-08-31 09:07 – 09:51（clean build G1 轮）** |
| 验收方式 | 实时 MCP 对话式调用（非 pytest、非脚本、非代码扫描） |
| 代码基线 | 首轮 `ddb822a`；**8-31 轮 `4c36996096f6fe9556d42318ff74b15967f37199`（工作区 clean）** |
| `build_commit` 实测 | 8-31 轮所有信封回报 `4c36996`，与 HEAD 一致 |
| `build_metadata_complete` | 首轮 `false`（dirty → dev build only）；**8-31 轮 `true`，`build_time=2026-08-31T00:56:10Z` → 满足 G1+ 资格** |
| 验收工作区 | `acc20260830`（首轮）；**8-31 轮 `acc20260831b` 主链 + `src0831`/`acq0831`/`ref0831`/`zmd0831` 四条并行链** |
| 覆盖服务 | 14 / 14 |
| 项目案例 | 江夏区智能装备制造基地新建项目（湖北省武汉市江夏区，machinery） |

> **本文档状态**：第二节起为 8-30 轮记录，其"未发现新增阻断性 P0/P1"的结论**已被 8-31 clean build 轮推翻**。当前有效结论见下方「2026-08-31 clean build 轮」与文末第七节。

## 2026-08-31 clean build 轮（当前有效结论）

首轮修复在 clean build 上**全部真实成立且未误伤正常链**；同一轮又发现 **4 个新 P0 + 7 个新 P1**，均已在本轮修复并进程内验证（见第四节）。

> **累计状态（截至 2026-08-31，第七节为最新）**：三批共修 **9 P0 + 22 P1 + 若干 P2**
> （P0 逐项：8-30 轮 2 个、8-31 clean build 轮 4 个、第二批 3 个——其中 1 个是我修
> 第一个 P0 时自己引入又修掉的 quick profile 死锁，按同等严重度计入）。
> 第一批 11 项已提交（`b13fe5c`/`d14156a`）；第二批 14 项在工作区**尚未提交**，
> 全仓回归 629 passed / 0 failed（两次独立运行一致）。剩余未修见第 7.5 节，其中无 P0/P1。

> **验收资格与验收结果是两件事**：本轮取得 G1+ 门禁资格（`build_metadata_complete=true`），但因发现 3 个可产出错误数字且不阻断的 P0，**发现阶段的验收结论为不通过**。修复后的复验须在提交、clean checkout、重启服务后重跑活体验收才能计入 —— 本文档的修复记录是进程内验证，不等于活体复验。

### 一、上轮修复复验（9 项全部成立，真实调用取证）

| 编号 | 实测证据 |
|---|---|
| P0-1 成本量级 | `conversion_to_wan:10000` 被 schema 层拒（`maximum of 1`），remediation 附 `Example: 0.0001` |
| P0-2 内联 spec | **扁平顶层**内联与 `spec_id` 路径产出逐项一致（68400 / 50% / 97680 / 56151 / IRR 45.36） |
| P1-2 硬编码 blocking | 量纲不可比时 `blocking=true`、`feasible=false`、`business_success=false` 存活到确认后；口径修正后 opt_a `feasible=true`、`violations=[]`，**fail-closed 未误杀正常交付** |
| P1-4 异常当结论 | `system_errors=[]`、`validation_complete=true`、无 error 维度；technical 透传 9 条真实 blocker；viability 读到 IRR 45.36 与逐年勾稽；standard 读到真实字段路径 |
| P1-5 绑定字段矛盾 | `actual_bound_run_id` = `bound_run_id` = `run_bd303cab866f`；结论为"不合格"时仍回报 `candidate_finance_tables_package_id` 与预览件 URI；结构消息指真因不再印"有效 11/11 张" |
| P1-6 审查假阳性 | 3 个旧假阳性消失（详见新 P1-N5：暴露出新的政策数字假阳性） |
| P1-7 首次落章 | propose 阶段即拒 `section_absent_from_document` |
| P2-2 remediation | 报错直接列 `Accepted keys: …` / `Required: …`，不再是空 `Example: {}`（实测 6 次撞墙全部一次定位） |
| P2-9 信封膨胀 | 107 findings 的 `warnings` 压成 2 行摘要 |

**数字绑定是真校验**：故意写入 IRR 88.88%、静态回收期 1.11、盈亏平衡点 5.00%，`finance_narrative` 三项全部以 expected-vs-found 抓出并追加 `finance_narrative_mismatch`。

**分级发布门禁全部正确拒绝**：`report_export_docx(formal_candidate)`、`review_export(xlsx)`、`review_finalize`（只认 ReviewPackage）、`feasibility_release(project_delivery)`（列 63 条阻断含 `project_fact_certification_required`）、`dr_submit` 伪造 locator 与自报质量数字（服务端复算推翻 `citation_coverage`/`query_rounds`）。

### 二、8-31 轮新发现缺陷

| 编号 | 严重度 | 问题 | 根因 |
|---|---|---|---|
| P0-N1 | P0 | **内联 spec 嵌套 `revenue` 被静默丢弃**：`revenue.annual_revenue_wan:97680` 换成 `20520`（=投资额×30% 派生基线），利润总额 40614.71 → **−35631 符号翻转**、NPV +164301.87 → −246330.44、IRR 无解，而 `business_success=true`/`consistency_ok=true`/`blockers=[]` | `spec_cases.py` 组提升白名单只有 `cost`/`tax`；`rail_validation.revenue_input_complete` 对 `model=="flat"` **无分支** |
| P0-N2 | P0 | **收购域利润表漏扣利息 + 资产负债表不平**：净利润=收入−成本−折旧−税（漏利息），差额恒等于利息列；光伏侧符号反转（+48.375 → 含息 −731.077）。资产负债表 Y1 差 559.60、逐年放大，而 `consistency_ok=true`。**已写入交付 CSV** | `monthly_engine.py` / `solar_engine.py` 各自独立漏项；`balance_sheet.py` 勾稽自检是同义反复，真正恒等式从未检验 |
| P0-N3 | P0 | **零材料域行业未解析路径整条不可用**：8 次建单 4 次返回 `invalid_tool_output`，23/23 常见行业全落此路径，`candidates` 与 `next_actions` 均空，自恢复路径被完全掩盖 | `intake.py` 错误路径自造 item 形状；`server.py` 的 `_MISSING_INPUT` 无条件必填四字段，四缺三 |
| P0-N4 | P0 | **`analysis_profile_tabular` 对任何表格资料都不可达**：正常解析的 XLSX/CSV 均返回 `insufficient_source_data` | `profile.py` 要求 `kind=="cell"` **且** 有 `sheet`；CSV 发 `cell` 无 sheet、XLSX 发 `spreadsheet_cell` 有 sheet，交集为空 |
| P1-N1 | P1 | **附表6 与附表6-1 工资口径不一致**：附表6 用输入福利 315（劳务合计 1365），附表6-1 按引擎默认 14% 重算成 147（合计 1197），差 168；且附表6 出现两行同名"工资及福利费" | `annual.py` 键匹配 `break` 于首个含"工资"的键，平行的"福利"键不被读取；`cost_item_tree` 勾稽不覆盖两表之间的 tie |
| P1-N2 | P1 | **`ota_commission` 双语义按取值猜**：`135`（schema 声明的万元语义）→ IRR −33.66%；`0.08` → IRR +5.52%，同字段两种量级两种结论且无 warning | `monthly_engine.py` 用 `0<=v<=1` 猜比率；schema 与 description 均未提及比率解释 |
| P1-N3 | P1 | **`confirmation_scope=process_acceptance` 被静默升格**：返回 `formal_input` + `formal_release_eligible:true`，请求的降级未体现在任何产物资格上 | `_is_process_acceptance_spec()` 定义为"依据无缺口"，依据不全时反而跳过降级分支 |
| P1-N4 | P1 | **`dr_submit` 结构性死锁**：`dr_add_sources` 强制字符串 locator，plan 校验要求字符串相等，citation 审计只解析结构化 dict —— 绑定来源后永远无法提交 | `schemas.py` 与 `registration.py` 两处 locator 契约不一致；`agent_lifecycle.py` 对 dict 做 `str().strip()` 比较 |
| P1-N5 | P1 | **审查侧政策数字假阳性**：7 条 `REPORT.NUMBERS.BOUND` 中 4 条把正文引用的省级规划目标（5,500 亿 / 1,000 亿）当作项目营收要求匹配 run；`management_cost` 的 `candidate_paths` 为 0 | P1-6 判据实测"为空"仍假阳性——判据只比键集合、不比正则宽严（`管理费用\|管理费` vs `cost_items\.管理`） |
| P1-N6 | P1 | **XLSX 公式单元格静默丢弃**：35 个 locator 缺 9 个公式格（合计/勾稽行），`status=succeeded` 无 warning | `source_files_repository.py` 把 openpyxl 的 `None`（未缓存公式）与"空单元格"同路径丢弃 |
| P1-N7 | P1 | **`geo_query(geocode)` 把行政区替换成无关 POI**：`武汉市江岸区`→`武汉市政府`、`武昌区`→`东湖`，`status:ok` 零 warning，偏差 12km | `map_geo/server.py` 用 `compact in address` 子串匹配 |

其余 P2：`skip_fields` 是死路（跳过决定不进 release_limitations 且被晋升门禁拒绝）、`reference_verify` 的 `as_of` 被回显但不参与判定、单字段 extract 取到邻项数字（分句过滤全空时回退未过滤集合）、`template_fill` 部分 row_key 拼错静默丢弃、CAGR 在跨季序列给出 216% 年化、resume 快照丢失 `report_profile`、`EXECUTION_DEFAULT_APPLIED` 批量假阳性、零材料 TemplatePack 正文无模拟来源披露。

### 三、8-31 轮 SKIPPED（不可伪造，必须诚实标注）

- `review_submit_assessment` 七域独立 Assessment 提交：要求 `independent_context=true` + 独立 `reviewer_context_id`；本会话既是出题者又自签七域等于伪造验收。
- 零材料 `sim_a_formal` 晋升成功路径：`internal.status` 只认 7 域 `review_confirm_dimension`，前置同上。因此晋升后产物诚实性未实测。

### 四、8-31 轮修复记录（4 P0 + 7 P1 + 1 连带缺陷）

> 本节为**改动与进程内验证**记录。哪几项已取得**活体**证据见第五节 —— 两者不可混为一谈；P0-N3 曾在此被误报为已修，实际首次修复无效。

| 编号 | 核心改动 | 验证方式与结果 |
|---|---|---|
| P0-N1 | `spec_cases.py` 组提升新增 `revenue`，但**仅在 `model=="flat"` 时**且只提升白名单交集键；`rail_validation.py` 补 `flat` 分支承认 spec 侧达产营收 | flat 提升 97,680 且 `revenue_input_complete=True`；tourism 保留调用方 12,000.3、`rejections=[]`（不误判舍入差） |
| P0-N2 | 三处引擎净利润补扣利息与税金及附加；维护性资本支出资本化进固定资产；处置当期资产出账 + 处置损益进损益；处置后维护支出转费用；`balance_sheet.py` 增加真正的 `total_assets == total_le` 检验（原两条为同义反复） | 酒店月度链 8/8 期恒等式成立（max delta 0.16，纯舍入）；光伏链 10/10 期成立（delta 0.0）；`projection_consistency_ok` 由 False→True |
| P0-N3 | `_MISSING_INPUT` 必填收窄至 `field`；补 `reason`；`candidates` 按**真实对象形状** `{industry_code, industry_label}` 声明（首版误写为字符串数组，修复无效，见第五节更正记录） | 按 `register_tool` 实际传入的同一 `_OUTPUT` schema 校验，化工/光伏歧义/钢铁/养老四条语句 `schema_errors=0`。**仍缺活体证据，须再重启确认** |
| P0-N4 | `profile.py` 接受 `cell`（CSV，无 sheet）与 `spreadsheet_cell`（XLSX，有 sheet）两种真实形状 | 两个 parser 的产物均可进画像 |
| P1-N1 | `annual.py` 不再在首个"工资"键 break，改为同时扫描平行的"福利"键；显式福利金额直接采用、不按 14% 重算 | 附表6-1 Y1 welfare 由 147 → **315**，劳务合计 1365 与附表6 一致，168 差额消除 |
| P1-N5 | `patterns.py` 7 条成本项路径正则改为允许前缀（`cost_items\.[^.]*?管理`） | 6 个真实 run 键各命中唯一 metric；`cost_items.原材料.管理` 不跨点号泄漏 |
| P1-N2 | `ota_commission` 判别规则保留（改判会静默重算历史 spec），但在 schema description 与运行 `assumptions` 双向披露实际采用的语义 | 填 `0.08` 回报「比率（客房收入×系数）」、填 `135` 回报「金额（万元/期）」；IRR 差异从此可解释 |
| P1-N3 | `specs.py` 的 `process_acceptance` 改为「调用方显式请求即生效」，依据完整性只决定是否附 `PROCESS_ACCEPTANCE_BASIS_INCOMPLETE` 质量提示、不再决定资格档位 | 请求 process_acceptance 得到 `confirmation_scope=process_acceptance` + `project_fact_certified=False` + `business_decision_status=not_selected`（修复前为 `formal_input` + `formal_release_eligible=true`，即请求越不合格资格越高） |
| P1-N6 | `source_files_repository.py` 新增 `_spreadsheet_formula_cells()`（`data_only=False` 二次打开），公式格保留 locator 并标注 `formula` + `value_unresolved=true`；任何失败退回空 dict，不影响原本可读的数值格 | 4 标签 + 3 数值 + 1 公式格 = 8 个 locator 全在；`B4=SUM(B2:B3)` 带 `value_unresolved=True`，「合计」标签不再孤立 |
| P1-N4 | `agent_lifecycle.py` 对结构化 locator 取寻址字段比对，不再用 `str(dict)` 的 repr；plan 侧完整地址与结构化侧 kind 互为前缀即视为同源 | 审计所需的结构化形状不再判冲突；指向**别的**来源仍正确报 `CONFLICT` |
| P1-N7 | `map_geo/server.py` 行政区级查询（以 区/县/旗 结尾）若库内无专属记录，直接返回未命中，不再落到 address 子串匹配 | `江岸区`/`武昌区`/`洪山区` → NOT FOUND（库内确实无该区记录）；**`江夏区` 仍正确解析**到县级行政中心；市级、精确名、POI 模糊匹配均不受影响 |
| 政策数字假阳性 | `claims.py` 新增 `_citation_scope()`：只看数字**左侧**最近的归属标记，引述标记（`全省`/`规划提出`/`力争到`/`统计公报` 等）命中且无更近的项目标记（`本项目`/`达产年` 等）时判 `external`；`entry.py` 对 external 数字改报 P2「未标注来源与口径」而**不静默放过** | 省级 5,500 亿 / 1,000 亿 → `external`；**故意写错的项目营收 55,000（且同句含"全省"）仍判 `(none)` 保留 P0 绑定门禁**——判据是归属标记而非数值大小，不会放过真的量级错绑 |

**过程中自己引入并已修正的一处回归**：P0-N1 初版未区分收入模型，把 tourism 链的 `12000.0`（量价折算回显值）与 `12000.3`（显式输入）判成 `candidate_input_conflict`，整条零材料链 fail-closed。这是「fail-closed 的真风险是误杀正常交付」的又一次实证——已收窄为仅 `flat` 提升。**教训沉淀**：修"静默丢输入"时必须枚举字段的全部合法书写形态，同时确认该字段在各分支里是**独立输入**还是**派生回显**，后者不可当输入提升。

API 快照基线因 `roll_annual_balance_sheet` 新增两个关键字参数（`maintenance_capex`、`disposal_period_index`，均带默认值、向后兼容）已重新生成；diff 经逐行核对仅含 4 处签名与时间戳，无其他 API 漂移。

**本轮 8-31 发现的 4 P0 + 7 P1 已全部修复**（11 项）。三处刻意保留的设计选择：

1. `ota_commission` 的取值判别规则**未改判**——改判会静默重算历史 spec，改为双向披露实际语义。
2. 引述外部口径的数字**未静默放过**——降为 P2 并要求正文标注来源，仍留痕可复核。
3. `analysis_profile_tabular` 的 CSV 归到默认表名而非伪造 sheet 名，不虚构工作表概念。

### 五、修复后活体复验（真实 MCP 调用，dev build）

workspace `acc0831fix`（前半）与 `acc0831g1`（提交后补验）。两次重启：12:08（`build_commit=4c36996`，覆盖 P0-N1/N2/P1-N3）与 12:44（`build_commit=b13fe5c`，即提交后，覆盖 P0-N3/N4/P1-N7）。

**本轮仍记 dev build，原因与上轮不同**：提交后工作树已 clean，但 `build_metadata.json` 仍记录 `4c36996` 而 HEAD 已是 `b13fe5c`，`build_metadata.py:253-256` 因 `stale_build_commit` 主动清空 `build_time`。这是"元数据未随 commit 重新生成即不得取得正式构建资格"的保护机制，不是缺陷。已运行 `scripts/write_build_metadata.py` 重新生成（`build_time=2026-08-31T04:46:54Z`，进程内解析确认 `complete=True`），但运行中的服务在启动时已解析一次快照（`BUILD_METADATA` 模块级常量），**需再重启一次才会生效**。

| 编号 | 修复前 | 修复后（活体实测） |
|---|---|---|
| P0-N1 | 营收 20,520 / 利润 −35,631 / NPV −246,330 / IRR 无解 | **97,680 / +40,614.71 / +164,301.87 / IRR 45.36%**；`revenue_driver_missing_using_default` 消失（`run_65e9892b2c15`） |
| P0-N2 | 光伏 Y1 差 979.46 且逐年放大；净利 +48.375（漏息） | **10/10 期 delta=0.00**；利息已扣；Y10 处置损益 −11,153.17 且资产出账（`acqrun_6141c176…`） |
| P1-N3 | 返回 `formal_input` + `formal_release_eligible=true` | **`process_acceptance` + `eligible=false` + `project_fact_certified=false` + `business_decision_status=not_selected`**，依据缺口以 warning 披露（`spec_b5fc8b70…`） |
| P0-N3 | 返回 `invalid_tool_output`，`candidates`/`next_actions` 全被吞掉，23/23 常见行业均不可用 | **`status=missing_inputs` + 9 个 industry_code 候选 + 可执行 next_action**；歧义句另带 `matched_keywords`（度假/酒店 vs 光伏）说明歧义原因（`zmi_d6cef4ea…` / `zmi_c461fc49…`） |
| P0-N4 | 任何已解析表格均报 `insufficient_source_data` | CSV(`cell` 无 sheet) 活体实测 **`status=ok`**：5 行×2 列、表头 `["项目","金额（万元）"]`、4 数值格/6 文本格，CSV 归 `csv` 表名不伪造工作表（`profile_226bf6eb…`）。XLSX(`spreadsheet_cell`+sheet) 形状为进程内验证 |
| P1-N6 | 44 格只留 35，公式格（合计/勾稽行）静默消失 | 2/2 公式格保留并标注 `formula` + `value_unresolved=True`，`degraded_reason` 为空（**进程内验证**，未取得活体证据） |
| P1-N7 | `江岸区`→`武汉市政府`（偏差 12km，零 warning） | **`map-geo.not_found`** 并披露覆盖范围限制；**`江夏区` 仍正确解析**到县级行政中心（未误杀） |

**P0-N3 的更正记录（重要，保留以示教训）**：首次修复**无效**，我曾据此误报"已修"。真因不是必填项，而是 `candidates` 的实际形状是**对象数组** `{industry_code, industry_label}`，我按字符串数组声明——用推断代替了核对真实载荷。第二次改按真实形状声明并保持 `additionalProperties` 放开（正因如此 `matched_keywords` 这个额外键才能透出）。当时那次失败调用发生在编辑之后、重启之前，**既不能证明修复无效也不能证明有效**；上表的活体结果来自第二次重启后的真实调用。

**活体链上发现并修掉的连带缺陷**：利润表 `income_tax_wan` 取项目口径（380.47），而净利润用含息口径（237.35），导致利润表逐行复算恒差两种税之差——表自身不平。已让两者同源（9 个运营年逐行复算完全相等，Y10 差额为处置损益），项目口径税另存 `project_income_tax_wan` 供现金流侧，未影响 `tax_calculation` 与项目现金流表。

**复验中确认仍存在的既有 P2**：收购 spec 的 `oneOf` 无判别式（上轮 D6）。填 spec 撞墙 3 次，报错始终只有"not valid under any of the given schemas"，真因（`asset_scope[0]` 缺 5 个必填字段、`status` 枚举不含 `in_use`）须逐分支二分才能定位。本轮未修。

### 六、G1+ 正式验收轮（2026-08-31 13:03 起，`build_metadata_complete=true`）

**首次取得 G1+ 门禁资格**：`build_commit=d14156a` = HEAD、`build_time=2026-08-31T04:55:20Z`、工作树 clean。

前两轮 dev build 的原因各不相同，均为保护机制而非缺陷：① 工作树 dirty；② 元数据记录旧 commit（`stale_build_commit`）。第二次是我的操作顺序错误——在 `b13fe5c` 上生成元数据后又提交了文档，HEAD 一变元数据立刻 stale。已在最终 HEAD 重新生成。

#### 6.1 `review_submit_assessment` 七域路径：本轮首次真正走通

前两轮记 SKIPPED 的路径已由**独立审查上下文**打通（`independent_context=true` + 独立 `reviewer_context_id`；主会话既造产物又验收等于伪造，故必须换上下文）。七域 assessment 全部提交成功，status 反映真实判断而非为走流程报 passed：

| 域 | status | 依据 |
|---|---|---|
| compliance / article_quality / data_quality / source_quality / financial_tables / feasibility | **failed** | 分项 11020 ≠ 合计 10000（差 1020 万元）、IRR 12.6% 零来源支持、"十三表"实为 5 行表名清单等 |
| financial_model | **not_determinable** | 仅 5 个参数、无收入/成本/现金流，IRR 无法重算——诚实报不可判定 |

**关键流程更正**：正确顺序是 `review_package_prepare → review_package_confirm → review_prepare(target_type=review_package) → review_start → review_submit_assessment`（`suite_review.py:885` 要求 Review 的 target 必须**就是**已确认 ReviewPackage）。此前文档里的顺序是错的。

#### 6.2 G1+ 轮新发现缺陷

| 编号 | 严重度 | 问题 | 根因（已独立复核） |
|---|---|---|---|
| G1-D1 | **P0** | **复测未整改完时父审查被永久锁死**：`review_retest` 后父审查（实有 12 条 findings）的 `review_disposition_finding` 一律返回 `review_not_ready` / "校验引擎尚未形成 findings"——错误码与事实完全相反。父审查进入不可逃逸状态：既不能处置 finding，`review_export` 也降级为 `review_validation_incomplete` | 三处联动：`events.py:276-281` 要求 `not pending_retest_operation_ids`；`disposition.py:61-62` 以误导性码阻断；`suite_review.py:1328,1371` 仅当子审查 `overall=="pass"` 才清挂起。已实测确认 `complete_pending_suite_retest` **只有这一个活调用点**，无其他清除路径。"复测=部分整改"是最常见路径，等于把常态变死局 |
| G1-D2 | P1 | `review_get_dimension` 重复返回每条语义 finding（同一 `finding_id` 出现两次），`finding_count=1` 与数组长度 2 自相矛盾；`review_list_findings` 不重复 | `suite_review.py:1210` 把 `state["findings"]`（已由事件投影）与 assessment payload 里的同一批 findings 拼接。消费方按数组长度统计 P0 会翻倍——与"10 个 P0 实为 1 缺陷×10 期"同类的重复计数陷阱 |
| G1-D3 | P2 | `review_prepare` 对 `review_package`+`external` 组合把 `review_purpose` 静默改写为 `process_acceptance`，与 `review_resolve_standards` 同参数返回的 `project_delivery` 不一致，且响应无改写警告 | `preparation.py:94-96` 的 `setdefault`。方向保守（外部套件不给正式资格）但跨工具口径不一致 |

**G1-D1 只在"复测 + 部分整改"这条真实路径上暴露**，前两轮因 `review_submit_assessment` 从未走通而不可能被发现。

#### 6.3 G1+ 轮实测正确的门禁

**伪造证据 4/4 被拒**：伪造 fragment → `citation_fragment_mismatch`（服务端按 locator 重解析原文比对）；越界 source_id → `assessment_evidence_not_in_package`；伪造 EvidencePack URI → `standard_evidence_resource_invalid`；幽灵 report_revision → `report_revision_not_found`。另 `expected_sha256` 抓到一次真实的字节级传输误差。

**独立性门禁三层**：`independent_context=false` 在 **schema 层**即被拒（`const: true`）；同一 `reviewer_context_id` 跨域复用 → `assessment_context_not_independent`；未确认草稿当 package 提交 → `review_package_not_found`。

**七域确实驱动 verdict**：空 assessment 时 finalize 报 7 个 `review_dimension_unconfirmed`；七域齐后逐域反映真实 status，`overall_verdict=incomplete`，`not_determinable` 单独列为 blocker。

**诚实性字段全部诚实**：`identity_or_credential_verified=false`（9 次确认全部，硬编码无路径置真）、`professional_signoff_determined=false`、`compliance_conclusion=not_determined`、`project_fact_certified=false`；`PKG-STD-011` 主动披露 `framework_only` 并警告"不得声称已按方法书全文完成"；正式 DOCX/XLSX 被 `FORMAL_ARTIFACT_QUALIFICATION_REQUIRED` 阻断而 json/markdown 放行；`confirmed` 不终结阻断性；`review_retest` 拒绝自动关闭语义 finding。

**单字段 extract 拒绝猜测**（上轮 P0-1 的 G1+ 复验）：只声明 `高端装备` 一个别名时，两个候选均 `numeric_value: null` + `rejected: unit_incompatible`——拒绝猜测而非抓邻项数字；换精确别名后两来源各自给出 5500 亿元、`attribution_gate: passed`、`value`/`numeric_value`/`excerpt` 同源。

#### 6.4 独立上下文的诚实声明

该上下文明确声明：**未认证任何项目事实**（findings 仅针对其为打通路径自建的 5 个核验材料，非真实项目资料）；**未做任何法律、执业资格、电子签名或合规审批层面的批准**，也不具备该资格。

`review_score_section` / `review_compare_assessments` 归 UPSTREAM_FAILURE：两者只消费 `report_revision` 派生的 RubricAssessment，而主会话本轮主链尚未推进到报告阶段，非工具缺陷。

### 七、第二批修复与深度代码审查（G1+ 轮后续，尚未提交）

本批修 **2 P0 + 8 P1 + 4 P2**，全仓回归 **629 passed / 0 failed**（两次独立运行结果一致，与修复前基线持平，未改测试掩盖缺陷）。

#### 7.1 两个 P0

| 编号 | 修复 | 实测验证 |
|---|---|---|
| 复测锁死父审查 | `events.py` 新增 `findings_available` 与 `validation_complete` 解耦；`disposition.py` 前置改用前者；`suite_retest.py` 内层守卫从 `overall=="pass"` 改为「七域是否评审齐」并**按 profile 分叉** | 审查+收购域 104 passed |
| 单字段抽取取邻项数字 | `numeric_gates.py` 把「分句过滤后为空则回退未过滤集合」改为**返回空集合**，让 Gate 2 判 `nearest_label_mismatch` | 错配场景 `numeric_value=None` + `rejected`；**4 个正常场景全部正确命中**（同分句、顿号并列句三项、单字段查自身项），未误杀 |

**关键判断**：没有放宽 `validation_complete` —— 它把关正式导出与发布。只放开「处置 finding」的前置，因为处置本身不授予任何正式资格。逐条关闭判据仍是 `new_finding is None and coverage_ok`，按 finding 独立裁决，部分整改不会误关未修项。

分析域那处原注释写「宁可后续门拒绝」，但该前提不成立：单位已匹配且调用方未声明限定词时，后面没有任何门会再拦，回退即最终判决。

#### 7.2 深度代码审查发现的新问题（均已修）

| 编号 | 严重度 | 问题与根因 |
|---|---|---|
| 审查-1 | **P0（我自己引入）** | 上述 P0 修复的**初版把死锁换了个位置**：新前置无条件要求七域齐全，但 `quick` profile 下 `require_semantic=False`、七域本就不必提交（`suite_review.py:1176,1203`），quick 复测会永远卡在 `retest_assessment_required`。已改为 standard/deep 查七域、quick 查 `findings_available` |
| 收购-1 | P2 | `EXECUTION_DEFAULT_APPLIED` 假阳性的**真根因是键名不匹配**（比先前描述的「默认值未生效」更精确）：`runs.py:109,111,123` 注入 `annual_utilization_hours`/`projection_years`/`revenue.annual_revenue_wan`，引擎读的是 `utilization_hours`(`solar_engine.py:49`)/`remaining_operating_years`(`:56`)/hotel-lease 驱动。grep 确认这三个键**全仓零消费方**——填了永不被读的字段还产出 low-confidence assumption，把真实缺口淹没在噪音里。已移除，并验证真缺字段时引擎仍 fail-closed 抛错 |
| 审查-2 | P1 | `review_get_dimension` 重复返回每条语义 finding（`suite_review.py:1210` 把已由事件投影的 `state["findings"]` 与 assessment payload 里同一批记录拼接），`finding_count=1` 与数组长度 2 自相矛盾。已按 `finding_id` 去重，保留带 history 的投影 |

#### 7.3 审查纠正的一个既有认知错误

记忆里「oneOf 每分支须 <2KiB」这条我**理解过严**，曾据此判断 D6 不可修。实测 `transport.py:66` 的 `_PUBLIC_SCHEMA_INLINE_LIMIT` **只作用于 tools/list 的公开投影**（超限时完整 schema 改由 Resource 发布，`:476-482`），**服务端校验一直用完整 schema**。收购 `_SPEC_SCHEMA` 两分支实测 20,420 / 11,680 字节，校验完全正常。该约束条目已补充边界说明。

#### 7.4 其余 8 项 P1/P2（本批已修，逐项实测）

零材料域 4 项（TemplatePack 正文披露、`skip_fields` 死路、跳过历史留痕、resume 丢 `report_profile`）：零材料域 **160 passed**。

参考+分析域 5 项，我逐项亲自复验行为：
- `reference_verify` 的 `as_of`：1990 年→`False`、2026 年→`True`、不传→`True`、非法日期→`None`（不猜），**四方向全对**
- 单位差异绕过期间门禁：万元 vs 亿元跨期→`partial` 且**同时**报期间与单位不可比；同单位年 vs 季仍 `partial`；完全同口径同值仍 `ok`（未误杀）
- 区间引用：`SUM(B2:B5)` 展开全 4 格（原只取 B2）；`A1:Z50` 报 `total=1300` 但只展 64 格且 `range_truncated` 可归因
- `template_fill` 三个子缺陷：拼错键报警且 `rows_used=1`、空 data `rows_used=0`（原恒为模板行数 15）、number 列收中文报警；**全对键时零告警**（未误报）
- `archive_get_template_paragraph` 空结果可诊断

收购域 `run_model` 接受未确认 spec：改判会破坏既有预览路线，故保留行为但**透出 `spec_confirmation_status` 并附警告**，同时修正互相矛盾的工具自述（描述写「仅消费已确认」而 `save_spec` 的 next_actions 写「或直接运行模型」）。

#### 7.5 仍未解决

| 编号 | 严重度 | 问题 | 修复方案 |
|---|---|---|---|
| D6 | P2 | 收购 spec 的 `oneOf` 无判别式：报错永远只有"not valid under any of the given schemas"，真因（`asset_scope[0]` 缺 5 必填、`status` 枚举不含 `in_use`）须逐分支二分才能定位 | 改 `allOf` + `if-then` 按 `asset_type` 判别，校验器只进匹配分支，`iter_errors` 即可给出精确 JSON Pointer，直接复用已有的 `field_errors` 机制（`service.py:152-190`，该能力目前完全被浪费）。已确认不受 2KiB 限制。**未实施原因**：会改公开契约并需重新生成 API 快照基线，风险面大于本批其余项 |
| — | P2 | `technical_preview_ready=true` 与 19 个工件不可用并存，且 18 个不可用工件的 `blocking_reasons` 全为空（`server.py:170` 定义了该字段却不填），逐工件不可诊断 | 填充 `blocking_reasons`；并把 `technical_preview_ready` 的判据与工件可用性对齐 |
| — | P2 | CAGR 在跨季度序列给出 216% 年化（`trends.py:130` 的 `_elapsed_years` 得 0.25，`:132` 只挡 `years<=0`） | 增设最小跨度门限（如 <1 年不算 CAGR），或输出时标注 span 过短不可年化 |
| — | 环境 | `LVKE_SOURCE_IMPORT_ROOTS` 指向不存在目录 `/Users/mac/Desktop/lvke-资料`，本地导入整条不可用 | 属**配置问题非代码缺陷**：`external_corpora.py:220-223` 抛错时未带上已算出的 `invalid_roots` 诊断，可补可诊断性 |

**仍为不可伪造 SKIPPED**：零材料 `sim_a_formal` 晋升成功路径（七域已打通，理论可达但本轮未做）。

### 八、8-31 轮产出（已落盘核验）

`lvke产出/acc20260831b/` 43 个文件：14 CSV（**14/14 带 UTF-8 BOM + 「正式候选·含限制」标记**）、1 XLSX（**恰好 13 附表，13/13 sheet 带限制标记**，中文以 XML 实体存储正常）、2 DOCX（独立解包实测 2 个内嵌 CJK 字体、2125 个中文字符、51 段落、限制正文在文件内）。另 `acq0831` 38 个、`zmd0831` 51 个文件。

## 修复状态总览（2026-08-31 08:30 更新）

首轮验收发现的全部问题已修复，并在服务重启后用**真实 MCP 调用**复验。

### 8-31 活体复验实证（重启后，`build_commit=ddb822a`）

| 验证项 | 昨天（修复前） | 今天（修复后，真实调用） |
|---|---|---|
| P0-2 内联 spec | 总投资 10000 / 35%-65% / "电力热力" / 营收×75% | **72237.94 / 51.08%-48.92% / machinery / 明细法 56385.5**，且直接产出 `consistency_ok=true` 的 run；`input_hash` 与 `spec_id` 路径完全相同（`71c8444beabf…`）→ 两条路径已真正等价 |
| P1-4 校验器 | `technical.status=error`、`details.error="unknown"`、viability 抛 KeyError | `technical.status=failed` 并透传 5 项真实 blocker、附 `actionable` 指引；`system_errors=[]`、`validation_complete=true`；viability IRR 37.42 / 22 条勾稽；standard 读到 13 个字段路径 |
| P1-6 审查假阳性 | 4 个 P0 里 3 个假阳性 | 3 个旧假阳性全部消失。**但暴露 2 个新假阳性**（我修复引入，已补修，见 P1-6 条目） |
| P2-9 信封膨胀 | 168 行 `质量提示：blocking_finding:*` | **2 行摘要**："55 个阻断 finding 未关闭；调用 review_list_findings 查看明细" |
| P2-1 行业清单 | 填 `machinery` 需先撞两次墙 | 一次通过 |

这轮复验的价值正在于**它抓到了单测抓不到的东西**：P1-6 的补修缺陷跨了两张模式表的一致性，纯函数断言与 71 项审查单测全绿，只有真实调用暴露。

| 类别 | 首轮发现 | 8-31 复验新增 | 修复 | 复核后撤销定级 |
|---|---|---|---|---|
| P0 | 2 | 0 | 2 | 0 |
| P1 | 7 | 0 | 7 | 0 |
| P2 | 13 | 3（P2-14 geo 字段名、P2-15 模板静默忽略、P2-16 知识入库错误码不分派） | 14 | 2（P2-4、P2-10 经复核不成立） |
| 门禁误判 | 1 | — | — | 1（`build_metadata` 缺失是保护机制，非缺陷） |

8-31 复验还补修了 P1-6 首轮修复的遗漏（正文模式表改了、run 路径表没同步），详见该条目。

**验证结果**

- 全仓回归：**629 passed / 0 failed**（修复前 625 passed / 4 failed）。最终一轮覆盖全部 30 个 `src/` 改动与 5 个基线文件，**零失败项**，用时 12 分 04 秒
- 行为验证：**52 项进程内断言** + **8-31 重启后的真实 MCP 调用复验**（P0-2 端到端、P1-4 四维度、P1-6 审查链、P1-1 导入→ingest→extract 全链、三个首轮未触达服务）
- 改动范围：**30 个 `src/` 文件** + 1 个测试断言 + 5 个基线文件（API 快照、lvke-reference 契约/工具清单/resources、manifest）
- 未修改任何测试以掩盖缺陷；唯一改动的测试断言是因语义变得更准确（`error` → `not_determinable`）

**关键修复效果**

| 问题 | 修复前 | 修复后 |
|---|---|---|
| P0-2 内联 spec | 总投资变默认 10000 万元、行业变"电力热力"、成本率 75% | 与 `spec_id` 路径产出完全一致：72237.94 / machinery / 明细法 56385.5 |
| P0-1 成本量级 | 年成本 46.97 万亿 vs 年营收 9.77 亿仍 `valid=true` | 超总投资 100 倍即 `project_scale_inconsistent` 阻断，并指出 `conversion_to_wan` 方向 |
| P1-4 校验器异常 | `blockers:["viability_validation_error"]`、KeyError 字符串泄漏 | `system_errors` 单列、`conclusion_available=false`；IRR 37.42 / 22 条勾稽 / 13 个字段路径全部读到 |
| P1-6 审查假阳性 | 4 个 P0 里 3 个假阳性（原材料被判 `utility_cost`） | 16 个数字全部正确归属，新增 `raw_material_cost` |
| P2-9 信封膨胀 | 56 个 finding × 3 处 = 168 行无信息文本 | 压成 2 行摘要 + 查询指引 |

## 一、验收门禁自身状态：dev build，且这是设计使然

所有信封回报 `build_metadata_complete=false`，缺失字段 `build_time`。

按 `lvke-mcp-acceptance` Skill 的 Build gate 规定（"`build_metadata_complete=true` required for G1+ acceptance"），**本轮只能记为 dev build 验收**。下述结论均在此前提下成立。

**复核结论：这不是缺陷，不需要修。** 首轮报告曾建议"单独修 build_time 缺失"，该建议已撤销。实测根因在 [build_metadata.py:257-259](src/lvke_mcp/runtime/build_metadata.py#L257-L259)：

```python
if not _tracked_worktree_clean(root):
    build_time = ""
    reasons.append("tracked_worktree_dirty")
```

`build_metadata.json` 里**一直有** `build_time`（实测 `2026-08-30T14:30:51Z`），但只要工作树存在未提交改动，解析器就主动清空它，并在 `source` 字段标注 `tracked_worktree_dirty`。这正是"未提交的改动不得取得正式构建资格"的保护机制，与 Skill 的"dirty checkout → dev build only"一致。

提交后 `build_metadata_complete` 会自动恢复为 `true`。要取得 G1+ 资格，需在 clean checkout 上重启服务后重跑一轮活体验收。

## 二、缺陷清单

严重度口径：P0 = 可产出量级错误或与输入无关的数字且不阻断；P1 = 状态/结论自相矛盾或批量假阳性；P2 = 可诊断性与契约一致性问题。

### P0-1 成本量级零对账，`conversion_to_wan` 语义与字段名相反 — ✅ 已修

**现象**：`planning_calculate_cost_drivers` 传 `conversion_to_wan: 10000` 时，原材料年成本算出 `4,696,800,000,000` 万元（约 46.97 万亿元），年运营成本合计 `4,696,800,002,400` 万元，而该项目年营收仅 `97,680` 万元 —— **成本是收入的 4800 万倍**。

该结果仍以 `business_success=true`、`status=calculated`、零 blocker、零 warning 正常固化为不可变对象（`costdrv_b473f8053ef39c60b1b30579`），可直接喂给 FinanceSpec 生成十三表。

**根因**：
- [`_lifecycle/cost.py:72`](src/lvke_mcp/servers/lvke_project_planning/_lifecycle/cost.py#L72) 默认值 `Decimal("0.0001")`，即元→万元的正确系数（÷10000）。但字段名 `conversion_to_wan` 读作"到万元的换算系数"，自然理解是 10000。schema 只有 `exclusiveMinimum: 0`，无描述、无上界。
- 收入侧有量级锚点，成本侧完全没有对账。对比 `quality_severity.py` 里 `project_scale_inconsistent` 是 `BLOCKING_CODES` 首项，注释写明"投资额与行业量级不符 → 后续十三表、报告、审查全部建立在错误基准上"，成本侧缺同等门禁。
- `planning_validate(cost_drivers)` 对 46.97 万亿一字不提，只报 `/invest_breakdown/construction_wan`，且 warning 明说"候选仍可确认和进入下游"。

**修复**（3 处）：
- [schema_parts.py](src/lvke_mcp/servers/lvke_project_planning/_server/schema_parts.py) `conversion_to_wan` 加 `maximum: 1` + `default: 0.0001` + 明确 description（"是**乘数**，不是「1 万元 = 10000 元」的进率；填 10000 会把金额放大 1 亿倍"）。schema 层直接拦住 10000。
- [cost.py](src/lvke_mcp/servers/lvke_project_planning/_lifecycle/cost.py) `validate_cost_drivers` 新增成本量级对账：年运营成本超项目总投资 100 倍即报 `project_scale_inconsistent:annual_operating_cost_vs_total_investment`，`field_errors` 给出实际比值与"通常是 `conversion_to_wan` 方向用反"的定位提示。
- [quality_severity.py](src/lvke_mcp/runtime/quality_severity.py) 该码经 `BLOCKING_PREFIXES` 判为阻断，同时把原先硬编码的 `blocking: False` 改为调用 `is_blocking()`。

**复验**：量级码进 `blockers`、普通码不进；`conversion_to_wan` 上界与 description 生效。

### P0-2 `finance_run_model` 内联 `spec` 路径静默丢弃全部输入 — ✅ 已修（⚠️ 嵌套 `revenue` 形态遗漏，见 P0-N1）

**现象**：同一份 spec 内容，走 `spec_id` 正常（总投资 73,316.04 读入）；改传内联 `spec` 参数则总投资变默认 `10000` 万元、资金结构变默认 35%/65%、行业变"未指定(默认:电力、热力生产和供应业)"、`invest_type` 空，只留一条不阻断的质量提示 `total_investment_missing_using_default`，然后照算出完整十三表。

**根因**：[`_model_application/run_cases.py:185-188`](src/lvke_mcp/domains/finance/_model_application/run_cases.py#L185-L188) —— 计算实际消费 `input_revision`（由 `finance_prepare_spec` 生成），内联 spec 路径下 `payload` 为空导致 `input_revision={}`。

同一函数内城轨是 fail-closed（`:199` 注释"缺失即 missing_inputs 且不建 run，而不是照算并附一条质量提示"），通用行业却 fail-open。

**修复**：[run_cases.py](src/lvke_mcp/domains/finance/_model_application/run_cases.py) 在 `input_revision` 为空且有内联 spec 时，复用 `finance_prepare_spec` 用的同一个 `_canonical_candidate_inputs()` 从 spec 派生 `input_revision`；派生过程若发现字段冲突则 fail-closed 返回 `candidate_input_invalid` 并给出 `field_errors` 与两条替代路径，而不是照算。

**复验**（端到端真实链路，同一份 spec 走两条路径对比）：

| 指标 | 修复前（内联 spec） | 修复后（内联 spec） | `spec_id` 路径 |
|---|---|---|---|
| 总投资 | 10000（默认） | **72237.94** | 72237.94 |
| 资金结构 | 35% / 65%（默认） | **51.08% / 48.92%** | 51.08% / 48.92% |
| 行业 | 未指定(默认:电力、热力) | **machinery** | machinery |
| 达产年总成本 | 73260（营收×75%） | **56385.5**（明细法） | 56385.5 |

两条路径现在完全一致。

### P1-1 事实候选 `value` 与 `numeric_value`/`excerpt` 可来自原文不同段落 — ✅ 已修

**现象**：`analysis_extract_candidates` 产出的 `cset_c7b64d4133a0e689a8c359ed` 中 `candidate_002`：

- `value` / `original_value` = "2016至2020年，全省规上工业企业利润总额分别增长9.6%、10.0%…6.18%"
- `numeric_value` = `2500`，`excerpt` = "力争到2025年，全省新能源与智能网联汽车产业集群实现营业收入2500亿元"（offset 9035）
- `attribution_gate.status` = `passed`

一条候选事实里，人读的 `value` 与机器算的 `numeric_value` 指向不同事实。下游按 `value` 复核、按 `numeric_value` 计算 → 复核通过但算错数。对象自述边界是"候选保留原值、原文与 locator"，但该候选的"原值"与"原文摘录"互不对应。

**修复（两部分）**：

1. [candidate_extract.py](src/lvke_mcp/servers/lvke_data_analysis/_service/candidate_extract.py) 把 `original_value` 的取值 offset 从"别名首次出现处"改为"三道门实际定位到数字的 `measure_offset`"，与 `numeric_value`/`excerpt` 统一同源。
2. **8-31 补修**：真实链路复验时发现同源性虽已修好，但 `numeric_value` **本身仍可能是别项的数** —— 查"水电能源"得到 `46968`（原材料的值），`attribution_gate` 还报 `passed`。根因是 [numeric_gates.py](src/lvke_mcp/servers/lvke_data_analysis/_service/numeric_gates.py) 的 Gate 2 用 `_span_distance` 纯字符距离找最近标签、不看分句：并列句里"水电能源"离 46,968 只有 13 字符，离它自己的 1,600 有 19 字符。已加入分句过滤（`_CLAUSE_MARKERS`，含顿号），全被过滤时退回原集合让单位/限定词门裁决而非在此猜测。

**这与 P1-6 是同一个"缺顿号"根因，但分布在两个服务的两套独立实现里** —— 8-30 只修了审查侧（`normalize.py`），分析侧（`numeric_gates.py`）到 8-31 真实调用才暴露。教训：发现语义配对缺陷时要搜全仓是否有第二份实现。

**复验**：真实导入文档 → ingest → extract，三项成本（原材料 46968 / 水电 1600 / 修理 800）全部正确归属，`value`/`numeric_value`/`excerpt` 同源。

### P1-2 建设规模确认把口径非法压成非阻断，顶层 `feasible` 由 false 翻 true — ✅ 已修

**现象**：市场案例单位"亿元"与产能单位"套/年"不相容，求解时正确判 `feasible=false` + `market_capacity_unit_mismatch`。但确认后同一对象顶层变 `feasible=true`，违规项降级为 `release_limitations`，`blocking=false`。

**根因（三处）**：
- [`_lifecycle/build_scale.py:377`](src/lvke_mcp/servers/lvke_project_planning/_lifecycle/build_scale.py#L377) `"blocking": False` 是硬编码字面量，完全绕过 `quality_severity` 分类器
- [`_service/factories.py:392`](src/lvke_mcp/domains/project_planning/_service/factories.py#L392) `blockers=[]` 硬编码
- 同文件 `:396` `feasible=not failures` 只算本次重算约束，不含上游传入的 `quality_issues`

`market_capacity_unit_mismatch` 也未登记进 `BLOCKING_CODES`（但登记了也会被 `:377` 压掉）。这是 `46a64b2` 质量契约重构在建设规模域的未清理残留。

**修复**（3 处）：
- [build_scale.py](src/lvke_mcp/servers/lvke_project_planning/_lifecycle/build_scale.py) `"blocking": False` 改为 `is_blocking(str(code))`
- [factories.py](src/lvke_mcp/domains/project_planning/_service/factories.py) `blockers=[]` 改为 `split_quality_codes()` 的真实结果；`feasible=not failures` 改为 `not failures and not combined_blockers`；`success`/`status` 随阻断项联动
- [quality_severity.py](src/lvke_mcp/runtime/quality_severity.py) 登记 `market_capacity_unit_mismatch` 进 `BLOCKING_CODES`，并注明"量纲不同的两个数不能相互校验，属口径非法而非置信度不足"

**复验**：该码判为阻断、结构性码（`finance_spec_pending` 等）仍不阻断——fail-closed 没有误伤正常链。

### P1-3 `cost.cost_items` 嵌套形态被静默忽略，回退 75% 成本率 — ✅ 已修

**现象**：`finance_validate_spec` 接受 `cost.cost_items`（返回 `valid=true`），但引擎只读顶层 `cost_items`（白名单在 `domains/finance/parameter_resolver.py:37`）。传嵌套形态时模型回退"总成本费用率按 75% 估算"+"工资按现金经营成本 15% 估算"，算出 `op_cost=73260`（=营收×75%），真实值应为 50,733。错值扩散到响应内 10 处。

`raw.spec.cost.cost_items` 里 5 项完整存在，`raw.cost_items` 只剩 `{"估算期现金经营成本": 0}`。

**修复**：[spec_cases.py](src/lvke_mcp/domains/finance/_model_application/spec_cases.py) 的 `_canonical_candidate_inputs()` 增加业务分组提升：`cost` 与 `tax` 两组里命中财务输入白名单的子键会被提升到顶层（`revenue` 不在此列——它有自己的 model/products 结构，由收入模型消费）。两侧同名且值不同时仍 fail-closed 报 `candidate_input_conflict`。

**复验**：`cost.cost_items` 与 `tax.vat_rate` 均被正确提升；真冲突仍被拒绝。此修复还暴露出 `sim_a_formal` fixture 里长期并存的两份不同成本口径（场景工厂四项 vs 国标四项），已同步修正 fixture。

### P1-4 `finance_validate_post_generation` 把内部异常当成校验结论 — ✅ 已修

**现象**：对 `consistency_ok=true` 的干净 run（`run_7cf8e2bf017d`）跑 `scope=formal`：

- `technical` 维度：`status=error`，`details.error="unknown"`
- `viability` 维度：`status=error`，`details.error="'investment'"`（Python KeyError 的字符串形式泄漏）

两者被包装成 `blockers: ["technical_validation_error", "viability_validation_error"]`，且 `system_success=true`。调用方会读成"校验发现技术/生存能力问题"，实际是校验器自己崩了。

同一次调用 `standard` 维度 5 项全部 `finance_paths_present=[]`，而该 run 确有 `total_investment_wan` 等字段（`finance_prepare_spec` 对同内容报 present 有值），说明读 run 的路径也不对。

**修复**（4 处，[post_generation_validation.py](src/lvke_mcp/domains/finance/post_generation_validation.py)）：
- `_error_result` 改为输出 `system_errors` + `conclusion_available=false`，不再进 `blockers`；带 `exception_type` 便于定位
- `technical` 维度在 `tables_validate` 返回 `success=false` 时**原样透传其 blockers**，不再抹成 `technical_validation_error`
- `_validate_viability` 数据源从"持久化 run 记录直喂 `check_consistency`"改为读 run 视图：`summary` 取 indicators、`checks` 取已固化 consistency（不重算）
- `standard` 维度的 `finance_inputs`/`table_manifest` 缺省时自动回退到 run（`full` 视图取 `input_revision`、`summary` 取 `table_manifest`）
- 顶层新增 `system_success` / `system_errors` / `incomplete_dimensions` / `validation_complete`

**复验**：`system_errors=[]`、无 error 状态维度、viability 读到 IRR 37.42 与 22 条勾稽、standard 读到 13 个字段路径。run 不存在时返回 `not_determinable` 而非 `error`，且不进 `blockers`。

### P1-5 `report_validate.finance_binding` 三处字段自相矛盾 — ✅ 已修

对 `rrv_35761b950b5c48176bf9b9d3` 实测：

1. blocker `finance_reference_structure_incomplete` 的 message 是"财务附表不完整：**有效 11/11 张**" —— 分子分母相同却判不完整，且 manifest 明确是 13 张
2. 同一响应内 `bound_run_id="run_7cf8e2bf017d"` 而 `actual_bound_run_id=null`、`finance_binding.finance_tables_package_id=null`，但顶层 `finance_tables_package_id` 有值
3. 补导 XLSX 后 `xlsx_resource_missing` / `xlsx_resource_uri=null` 仍在，`report_get_readiness` 同样读不到

**修复**（[gate.py](src/lvke_mcp/domains/finance/gate.py)）：
- `actual_bound` 此前从初始化到 return 从未被赋值，现按实际读到的 `run_view.run_id` 回填
- 结构不完整的消息不再印"有效 N/M 张"（N==M 时自相矛盾），改为指向真因：`depth_ok` 与逐表必需行列缺失，并把 `blocking_missing_by_table` 明细带出
- package 未通过门禁时也回报 `candidate_finance_tables_package_id` 与预览件 URI——诊断信息不该因为结论是"不合格"就消失
- 新增 `xlsx_formal_export_missing_technical_preview_only`：区分"还没导出"与"只导了 technical 预览件、需按 formal 重导"

**复验**：`actual_bound_run_id` 与 `bound_run_id` 一致；候选 package ID 与预览件 URI 均回报；消息读作"结构未达交付深度：逐表深度未达标；8 张表存在必需行列缺失"。

### P1-6 审查侧指标语义错配，批量制造假 P0 — ✅ 已修

4 个 P0 findings 里 **3 个是假阳性**，同一根因：按数字出现顺序而非紧邻标签绑定 metric。

| finding | 判定 | 实际 |
|---|---|---|
| `fnd_f46f3d99` | `utility_cost=46968` 无法复现 | 46968 是原材料；`candidate_paths` 全指向 `cost_items.水电能源`（=1600） |
| `fnd_24f465e4` | `utility_cost` 口径不一致 values=[1600,46968] | 把原材料与水电都归为 `utility_cost` |
| `fnd_894455cd` | `civil_cost=54000` 无法复现 | 54000 是工程费用合计；土建实为 18000 |
| `fnd_b6daa9ab` | `npv=90064.21` 无法复现 | **真阳性**：Monte Carlo P5，不在 `run.indicators` 内 |

正文原句"主要原材料 46,968.00 万元、水电能源 1,600.00 万元"两项名值一一对应且与 run 完全一致。引擎数字正确，缺陷在审查侧。

与已知的 `review-ebit-recalc-omits-amortization`（10 个 P0 是 1 个审查侧公式缺陷×10 期）同型。

**修复**（[normalize.py](src/lvke_mcp/servers/lvke_deliverable_review/_report_checks/normalize.py) + [patterns.py](src/lvke_mcp/servers/lvke_deliverable_review/_report_checks/patterns.py)）：
- 根因是分句边界缺顿号：中文成本/投资明细正是用 `、` 并列的，"水电能源"这个标签能跨过顿号绑到前一项的 46,968 上。现把 `、`、`：` 加入边界；括号按方向拆分（`（` 只作起点、`）` 只作终点），避免括注内分项与括号外合计互抢标签
- 补齐模式表实际写法："工程费用"（不带"合计"）、"修理与维护"、"工资"、"福利"，并新增 `raw_material_cost`（同步登记进 `_FINANCIAL_METRICS`、单位相容表与 `_PATH_PATTERNS`）
- `engineering_cost` 加 lookbehind 排除"建筑工程费/安装工程费"，避免抢走 `civil_cost`/`installation_cost` 的数值

**复验**：三段真实正文共 **16 个数字全部正确归属**（成本 6 项、投资明细 6 项、投资构成 4 项），此前的 3 个假 P0 消失。

**8-31 clean 复验补修（重要）**：对同一 revision 重跑真实审查时，3 个旧假阳性确认消失，但**暴露出 2 个新的假阳性** —— `welfare_cost=315` 与 `maintenance_cost=800`。根因是我上一轮只补了 `_METRIC_PATTERNS`（正文识别），没同步补 `_PATH_PATTERNS`（run 侧取值路径），于是这些数字从"不识别"变成"识别出来但在 run 里找不到值"，照样报 `REPORT.NUMBERS.BOUND`。

已补齐 6 条路径模式：`salary_cost`/`welfare_cost` 增加 `cost_items.工资|福利` 分支（原先只认 `annual.wage[*]`，那是按工资率推算的分年值，与用户显式给的金额不是同一个数）；`maintenance_cost` 增加"修理与维护|修理"；`civil_cost`/`equipment_cost`/`installation_cost` 三项此前**完全没有**路径模式（昨天 `civil_cost` 的假阳性正是此因，不只是分句边界）。

新增可复用判据：`(set(_METRIC_PATTERNS) & _FINANCIAL_METRICS) - set(_PATH_PATTERNS)` 必须为空——现已为空。

**方法教训**：这个缺陷跨了两张表的一致性，纯函数断言与 71 项审查单测**全部通过**却没抓到，只有真实 MCP 调用才暴露。修改这类"成对表"后必须走活体复验。

### P1-7 `report_propose_section` 首次落章不可用，且错误码误导 — ✅ 已修

outline 已固化 8 个 `section_id`，但正文尚无对应标题时（`report_get_section` 返回 `found_in_document=false`），propose 会成功创建提案，直到 apply 才报 `section_patch_stale`（"内容已变化或章节绑定失效"）。

真实原因是"正文里没有这个标题"，与"内容已变化"是完全不同的处置。propose 阶段即可判定却不拒绝；首次落章只能改走整篇 `report_propose`。

**修复**（2 处）：
- [sections.py](src/lvke_mcp/domains/reports/_service/sections.py) `propose_section` 在 `found_in_document is False` 时**直接拒绝**（`section_absent_from_document`），并指明首次落章走 `report_propose`——propose 阶段就能判定，不必等到 apply
- [revisions.py](src/lvke_mcp/domains/reports/_service/revisions.py) apply 侧把原先共用一个码的三种情况拆开：`section_not_in_outline`（章节不在 outline，要改 preparation）、`section_absent_from_document`（正文无该标题，要走整篇 propose）、`section_patch_stale`（内容确实被改过，要重新提案）

**复验**：propose 侧提前拒绝生效；apply 侧三码分离。

### P2 类问题汇总

除注明外均已修复。

| 编号 | 问题 | 状态 |
|---|---|---|
| P2-1 | `planning_get_industry_constraints` 的 `supported_industries` 只列中文键（机械/制造/能源…），实际两侧都通的是英文码（`machinery` → `matched_industry_key=机械`）。照清单填"机械"能拿到参数，但 `planning_resolve_industry_skill` 拒绝，把人引向死路 | ✅ 已修：新增 `supported_industry_codes`（英文码全集）与 `industry_code_map`（每个行业的可接受写法），两个 missing_inputs 信封都带 |
| P2-2 | schema 拒绝时 remediation 常给空 `Example: {}`，不指出合法键集。实测 3 次撞墙（`market_case` 的 `steps`、`labor_plan` 的 `selection_reason`、`policy_basis` 的 `confirmation_reason`）。对比 `evidence_binding_locator_mismatch` 会返回 available_locators 全文 + 两条替代路径 | ✅ 已修：[transport.py](src/lvke_mcp/runtime/transport.py) 对象校验失败时追加 `Accepted keys: …` 与 `Required: …`。改一处惠及 14 服务全部入口 |
| P2-3 | `planning_confirm` 7 个分支键名不统一：5 个用 `selection_reason`、2 个用 `confirmation_reason`；policy 用复数 `selected_candidate_ids` | ✅ 由 P2-2 覆盖：键名仍按历史 idempotency 命名空间保留（改名会破坏既有幂等记录），但报错现在直接列出该分支的合法键，不必再试错或读源码 |
| P2-4 | ~~聚合入口 `planning_prepare(market_case)` 未暴露底层必填 `evidence_pack_id`~~ | ⚠️ **撤销定级**：复核公开 schema 实测**含** `evidence_pack_id`（17,193 字节，未被 2KiB 裁剪）。首轮是我漏读——当时报错优先命中 `steps 不允许`，掩盖了真正缺的必填项。真实问题是"错误优先级掩盖根因"，已由 P2-2 覆盖 |
| P2-5 | `construction_wan` 必须等于 civil+equipment+installation+other+reserve 之和（`cost.py:162-166`），违反时只报 `invalid_or_missing_value`，不说这条勾稽规则 | ✅ 已修：改报 `investment_breakdown_not_reconciled`，`field_errors` 带 `rule` 表达式、`observed`/`expected`/`difference` 与五项构成明细 |
| P2-6 | `finance_prepare_spec` 返回的 spec 含 `investment` 字段（值 null），但 `finance-spec-v3` schema 不接受 `investment`（投资字段是扁平的）。工具返回值无法直接回填给 `finance_validate_spec` | ✅ 已修：候选 schema 显式声明 `investment`（12 个细项字段与 dataclass 完全对齐），并注明它与扁平 `invest_breakdown` 口径不同、不参与 input_revision 提升。往返已自洽 |
| P2-7 | `tables_export_xlsx` 描述写"默认 formal，正式门禁不过即拒绝"，实际是不过则降级放行：`tables_validate(formal)` 判 8 项 blocker，导出工具把同样 8 项全转成 warnings/`release_limitations`、`blockers=[]`，照样出文件。行为本身可接受（文件内嵌"【正式候选·含限制】…不得作为对外正式交付物"），但描述误导 | ✅ 已修：xlsx/csv 两处工具描述与 `validation_scope` 字段描述均改为如实表述「门禁未过时降级为『正式候选·含限制』并写入文件内标记，不拒绝导出」 |
| P2-8 | 流动资金收敛无求解入口。`working_capital_inconsistent` 是周转天数与营收/成本的循环依赖不动点，工具只反复报差额（12000 → 13816.04 → 12737.94），需调用方手工迭代。本轮迭代 2 次收敛 | ✅ 已修：勾稽项新增 `resolution`，直接给出应回填的 `working_capital_wan` 与随之调整的 `total_investment_wan`，一次即可收敛 |
| P2-9 | `review_start` 的 56 个 finding ID 在 `quality_issues`、`release_limitations`、`warnings` 各重复一遍（共 168 项），撑爆信封而无实质信息；ID 列表不说明问题是什么 | ✅ 已修：新增 `_quality_warnings()`，warnings 只留可读摘要 + 查询指引，明细保留在 `quality_issues` 一处。实测 56 个 finding 从 **168 行压到 2 行** |
| P2-10 | ~~`acquisition_validate_spec` 顶层 `valid=true` 而 `schema_valid=false` 并存~~ | ⚠️ **撤销定级**：`test_solar_candidate_reports_missing_model_start_date_without_blocking` 明确断言这两者并存，是刻意设计——`valid` 表示「校验已完成、诊断可用」，`schema_valid` 才是契约是否成立，正式资格看 `formal_valid`/`formal_release_eligible`。我曾改成 `valid=schema_valid`，被该测试正确拦下并已回退。仅在代码注释中补明三者语义差异 |
| P2-11 | `delivery_create_from_sentence` 把整句话填进 `project_name`，而 `region` 为空 —— 句中"武汉市江夏区"未被抽出 | ✅ 已修：`_project_name` 剥离请求语前缀与交付物名后缀；新增 `_region_from_sentence` 逐级扫描省/市/区（每级从上一级末尾起，避免拼出「武汉市汉市江夏区」），只接受至少两级、抽不到返回空不硬凑 |
| P2-12 | `report_prepare` 同时收到 `finance_binding`（新契约）与 `run_id`/`finance_tables_package_id`（旧契约）时报 `legacy_finance_binding_ignored` 并阻断，但不说该删哪个字段 | ✅ 已修：`_build_report_prepare_next_actions` 增加该码分支，明确「请删除顶层 run_id 与 finance_tables_package_id，只保留 finance_binding={kind, run_id, package_id}」 |
| P2-13 | `_legacy_tool_run_model`（`run_tools.py:49`）`return` 之后有 60+ 行不可达死代码 | ✅ 已修：删除 353 行不可达代码（第 51–404 行）。保留委托 return，`test_legacy_run_entry_delegates_to_governed_application` 仍通过 |
| P2-14 | **8-31 新发现**：`geo_distance_matrix` 的 `origins`/`destinations` items schema 是完全开放的 `{}`，未声明字段名。经度键实际叫 `lng`，传 `lon` 时坐标读不到、端点退化成按地名解析，报 `map-geo.not_found`（"请检查地名拼写或传 lat/lng"）——真正的问题是键名拼错，提示却把人引向地名 | ✅ 已修：items 加 `_GEO_ENDPOINT` schema（显式声明 lat/lng 及取值范围，description 注明"键名是 lng 不是 lon"）；服务层接受 `lon`/`longitude`/`latitude` 别名并做确定性归一化（已有 `lng` 时不覆盖） |
| P2-15 | **8-31 新发现**：`template_fill` 静默忽略结构不符的 `data`。静态行模板要求 `{row_key: {col_key: value}}`（row_key 形如 `construction.civil`），传 `{"rows": [...]}` 时每行 `data.get(row_key, {})` 全落空，整表渲染成空白却 `warnings=[]`、`rows_used=15` 看起来正常 | ✅ 已修：`data` 键与模板行键完全不匹配时给出明确 warning，并列出传入键、期望结构与 row_key / col_key 取值。正确结构与空 data 均不误报 |
| P2-16 | **8-31 新发现**：`knowledge_submit_candidate` 三类失败原因共用同一 `code`/`message`/`next_actions`（`knowledge_candidate_evidence_ineligible` + "补充 Resource、hash 和 locator"），只有 blockers 里的码不同。但"评估不存在"要去建评估、"评估未达标"要先改章节、"locator 缺失"才是补 Resource —— 处置完全不同 | ✅ 已修：按具体 blocker 分派 code/message/next_actions。评估未达标时 message 直接带 `weighted_score` 与 `passing`，next_actions 指向"先修订章节重新评分至 passing=true" |

## 三、实测正确的门禁（不应重复怀疑）

**证据分级与诚实性**
- 公开政府网页快照不自动升格：`formal_evidence_candidate=false`、`project_fact_certified=false`
- `dr_submit` 拒绝自由文本 locator（`citation_locator_not_found`）；服务端复算并推翻调用方自报的质量数字（`citation_coverage_mismatch`、`query_rounds_mismatch`），并标注"正文由调用 Agent 撰写，尚无独立 DR 质量审计"
- 行业参数返回 `evidence_eligibility=technical_fixture`，环保模板返回 `schema_only`
- `planning_resolve_industry_skill` 拒绝未知行业时明说"禁止静默使用通用行业默认值"

**口径优先于数值**
- market_case 两条路径算出完全相同的 9.768，仍判 `comparable=false`（地区口径不同：湖北省 vs 江夏区），不报"偏差 0%"，`aggregation=none`、`selection_required=true`
- `evidence_binding_locator_mismatch` 返回可用 locator 全文 + 两条替代路径

**不可变链与 stale 传播**
- `project_context_revise` 生成新 revision 并把下游标 `project_context_superseded`，旧对象只读
- `feasibility_stage` 推进生成新的不可变 run 快照，parent 指向原 run

**财务计算正确性（逐项复算）**
- 收入：81.4万×1200×0.6 = 58,608（首年）；达产 97,680
- 增值税：97,680×13% = 12,698.4；应纳 12,698.4−5,073.3 = 7,625.1
- 定员：42000/(350×2)×1.1×0.9 = 59.4 → ceil 60；9000/300 = 30
- 工资福利：60×10万+30×15万 = 1,050 万；×0.3 = 315 万
- 规模：60000/80000 = 0.75 容积率；32000/80000 = 0.4 覆盖率；12000/80000 = 0.15 绿地率
- 干净 run `run_7cf8e2bf017d`：22 条勾稽全过，`consistency_ok=true`

**资产负债表（本轮质量最高的输出）**
12 期全部 `balance_ok=true`、`equity_reconciliation_delta_wan=0`（账面权益 = 计算残差，无残差补平），`mismatch_periods=[]`、`cash_deficit_periods=[]`、`formal_ready=true`，带 `formula_lineage` 逐项公式来源。

**Monte Carlo**
500 样本全成功，IRR P5/P50/P95 = 27.74%/34.85%/40.99%，`samples_persisted=false`（只固化分位数），带 seed 与分布 manifest hash，可复现。

**十三表与导出**
- XLSX 恰好 13 张表（附表1~10 含 6-1/6-2/6-3），中文正常
- 14 个 CSV 全部 UTF-8 BOM（`efbbbf`），逐个带【技术预览·不可正式使用】标记，含 `00_数据血缘.csv`
- 附表3 诚实标注 `structure_coverage=0`、`row_tree_complete=false`、"ratio_backsolve 时不得宣称完整分项法"
- 附表5 底部注明"按综合率12%兼容预览；正式交付须确认项目所在地城建税率"

**报告链**
- `propose → diff → apply` 三步强制，缺 diff 直接拒绝
- `finance_narrative` 逐项复核正文 4 个数字与 run 完全一致（`mismatches=[]`）—— 数字绑定是真校验
- draft DOCX 内嵌 2 个 CJK 字体（NotoSerifSC-Regular 正文 / NotoSansSC-Medium 标题），`missing_cjk_glyph_count=0`、OFL 元数据齐全、`portable_cjk_fonts=true`，带各自 sha256
- 独立解包实测：2 个 `.odttf` 实际存在、26 段、753 个中文字符、首行"专家参考稿/内部复核·非报批终稿"、阻断项摘要写进正文

**分级发布门禁（`EXPECTED_REJECTION`，全部正确）**

| 调用 | 结果 |
|---|---|
| `tables_validate(formal)` 对 `consistency_ok=false` 的 run | `blocked`，8 项 blocker 含 `finance_run_consistency_failed` |
| `finance_build_balance_sheet` / `finance_run_monte_carlo` 对同一脏 run | `base_run_unavailable`，要求 `consistency_ok=true` |
| `report_export_docx(kind=formal_candidate)` | `FORMAL_ARTIFACT_QUALIFICATION_REQUIRED` |
| `review_export(formats=[xlsx])` 审查未关闭 | `FORMAL_ARTIFACT_QUALIFICATION_REQUIRED` |
| `feasibility_release(release_scope=project_delivery)` 阶段链 11/12 pending | `FORMAL_ARTIFACT_QUALIFICATION_REQUIRED` + 追加 `project_fact_certification_required` |
| `review_finalize` 对 report_revision | `review_package_target_required`（七域 finalize 只认 ReviewPackage，已知约束） |

`feasibility_validate(scope=formal)` 在 11 阶段 pending 时 `passed=true`，并非漏洞：阶段链未完成按"置信度不足"处理（`quality_severity.py` 的 `NON_BLOCKING_SUFFIXES` 刻意决定），真正的正式发布由 `feasibility_release` 拒绝。两级判定分工明确。

**审查准备**
14 条适用规则、2 个标准包（发改委 2023 大纲 + 固定资产贷款管理办法）带真实官方 URL 与 PDF sha256、`gate_status=passed`，自动解析全部上游绑定，并抓取 `legacy_gate_snapshot`（`verdict=fail`，与 `report_validate` 一致）。

**可诊断性达标的正面例子**
`feasibility_next_actions` 识别出工作区已有 `drp_84fd9921...` 可绑回当前阶段，并给出完整可执行参数（含 idempotency_key），照抄即成功。

**产品自报的规范缺口（诚实）**
> 2023 大纲要求财务计划现金流量表；当前十三表未设置独立同名表，只能由项目投资现金流量表和资本金现金流量表部分覆盖，正式版必须补充独立表或明确适用性说明。

## 四、两轨结论

**技术金标轨（修复后）**：本地实现、确定性计算、十三表、报告与审查接口在本轮范围内闭环。取得 `consistency_ok=true` 的干净 run，13 表 + 14 CSV + XLSX + DOCX 全部实际导出并独立核验。

首轮发现的 2 个 P0 与 7 个 P1 已全部修复，并在服务重启后用真实 MCP 调用复验（见"修复状态总览"的实证对比表）。其中 P0-2 的内联 spec 路径现在与 `spec_id` 路径产出**完全一致**的财务结果（`input_hash` 相同）。

**复验过程本身也发现了新问题**：P1-6 的首轮修复只补了正文识别侧、漏了 run 路径侧，真实审查立刻报出 2 个新假阳性。已补齐 6 条路径模式并建立可复用判据。这说明"单测全绿"不足以替代活体复验——该缺陷跨两张表的一致性，71 项审查单测无一抓到。

**在 8-30 轮覆盖范围内，技术金标链未发现遗留的 P0/P1。**

> ⚠️ **该判断已被 8-31 clean build 轮推翻**：在 clean checkout（`4c36996`，`build_metadata_complete=true`）上重跑并扩大覆盖后，新发现 4 个 P0 与 7 个 P1（见文首「2026-08-31 clean build 轮」第二节）。其中 P0-N1 与本节 P0-2 是**同一个白名单的第二次遗漏**——8-30 只修了扁平顶层形态，嵌套 `revenue` 分组仍被静默丢弃。这印证了下文方法教训 4「静默忽略输入」是本产品最高频的缺陷形态，且**修复必须枚举字段的全部合法书写形态**。

需要说明两点边界：一是 8-30 轮为 dev build（工作树带着这些修复、尚未提交，见第一节），G1+ 资格已由 8-31 轮取得；二是三个服务与若干场景 8-30 轮未触达（见第七节），未触达不等于通过。

**真实资料轨**：仅取得 2 份省级规划公开快照（湖北省经信厅装备制造业"十四五"规划、制造业高质量发展"十四五"规划），`extraction_receipt_verified=true`、`formal_use_allowed=true`。项目层面的投资、成本、定员、市场份额全部为 `technical_fixture` 行业基线，江夏区级产业地图与用地指标未取得原件。研究包 `partial`、项目事实未认证。**真实资料不足，保持 partial/blocked，未用金标结果解除任何正式门禁。此结论不因代码修复而改变——修复解决的是实现缺陷，不是资料缺口。**

## 五、修复记录与验证

修复顺序按首轮定的优先级执行。8-30 修首轮 22 个问题（25 个 `src/` 文件），8-31 clean 复验补修 4 个（再 5 个文件），合计 **30 个 `src/` 文件**。

| 顺序 | 问题 | 核心改动 | 复验方式 |
|---|---|---|---|
| 1 | P0-2 内联 spec 丢输入 | 复用 `_canonical_candidate_inputs()` 派生 `input_revision`；冲突则 fail-closed | 端到端跑真实 run，与 `spec_id` 路径逐项比对 |
| 2 | P0-1 成本量级零对账 | schema 加 `maximum:1`+description；validate 加量级对账并登记阻断码 | 构造 6568 万倍荒谬场景，确认进 blockers |
| 3 | P1-2 `blocking: False` 硬编码 | 三处改走 `quality_severity`；`feasible` 计入阻断项 | 阻断码与结构性码分别验证，确认无误伤 |
| 4 | P1-6 审查侧指标错配 | 分句边界加顿号、括号按方向拆分；补 5 类模式与 `raw_material_cost` | 三段真实正文 16 个数字逐一核对归属 |
| 5 | P1-4 异常当结论 | `system_errors` 单列；technical 透传真实 blockers；viability/standard 改读正确视图 | 干净 run 跑 formal，确认无 error 维度 |
| 6 | P1-5 绑定字段矛盾 | `actual_bound` 回填；候选 package 回报；结构消息指真因 | 实调 `assert_publish_finance_binding` 核对字段 |
| 7 | P1-1 value 与 numeric_value 不同源 | 原值 offset 改用三道门的 `measure_offset` | 并列文本同源性验证 |
| 8 | P1-3 / P1-7 / P2 组 | 见各条 | 52 项断言 + 全仓回归 |

**8-31 clean 复验补修（4 项，均由真实 MCP 调用暴露）**

| 问题 | 暴露方式 | 修复 |
|---|---|---|
| P1-6 遗漏：只补正文模式、没补 run 路径 | 对同一 revision 重跑审查，冒出 2 个新假阳性 | 补 6 条 `_PATH_PATTERNS`；建立"两表必须同步"判据 |
| P1-1 遗漏：同源了但 `numeric_value` 仍是别项的数 | 真实导入文档 → extract，查"水电能源"得到原材料的 46968 | `numeric_gates.py` Gate 2 加分句过滤（**同一"缺顿号"根因的第二份实现**） |
| P2-14 geo 字段名不可发现 | 传 `lon` 被当成地名解析失败 | items 加 schema 声明 + 服务层接受别名 |
| P2-15/16 静默忽略与错误码不分派 | 模板整表渲染空白零 warning；知识入库两类失败同一消息 | 分别加不匹配告警与按 blocker 分派 |

**验证纪律**：唯一改动的测试断言是 `test_validate_post_generation_never_blocks`——因为 run 不存在时的语义从 `error` 变为更准确的 `not_determinable`，并新增 `conclusion_available` 让调用方能区分"没有结论"与"校验器崩了"。其余首轮失败中，1 处（`acquisition_validate_spec` 的 `valid` 恒 True）是测试正确拦下了我的错误修改并已回退；1 处是 API 快照因删除未使用导入而更新；1 处是 `sim_a_formal` fixture 里长期并存的两份不同成本口径被 P1-3 修复暴露后同步修正。8-31 另更新了 `lvke-reference` 的 MCP 契约基线——该变更经核实为**纯增量**（删掉的只有两行空 `items: {}`，无 `required`、不禁额外属性，所有既有调用形态实测仍通过）。**没有为了让测试变绿而放宽任何门禁。**

**四条方法教训（值得沉淀）**

1. **"同函数内两套失败策略"是可扫的缺陷模式**。P0-2 的根因是同一个 `run_model()` 里城轨 fail-closed、通用行业 fail-open。已按此判据全仓扫过（见下"同类模式排查"）。
2. **修改"成对表"必须活体复验**。P1-6 的正文识别表与 run 路径表必须同步；只改一侧会把"不识别"变成"识别后报无法复现"，而这种跨表不一致纯函数断言与单测都测不到。已建立判据：`(set(_METRIC_PATTERNS) & _FINANCIAL_METRICS) - set(_PATH_PATTERNS)` 必须为空。
3. **同一语义缺陷可能有第二份独立实现**。"标签与数字配对时不看分句边界"这个根因在审查域（`_report_checks/normalize.py`）与分析域（`_service/numeric_gates.py`）各写了一套。8-30 只修了前者，后者到 8-31 真实调用才暴露。发现语义配对类缺陷时应先搜全仓有无同类实现。
4. **"静默忽略输入"是本轮最高频的缺陷形态**。P0-2（内联 spec 丢输入）、P1-3（嵌套 cost_items 被忽略）、P2-15（模板 data 结构不符）、P2-11（region 抽不到就留空）都属此类：输入没被采纳，但响应看起来正常。凡是"读不到就用默认值/留空"的分支，都应改为显式告知或 fail-closed。

**同类模式排查（8-31 补做）**

按上述判据扫了全仓，结论是**没有遗留的同类缺陷**：

| 判据 | 命中 | 结论 |
|---|---|---|
| 同函数内既有 fail-closed 早退、又有"用默认值继续" | 仅 `run_model()` 1 处 | 即 P0-2，已修；两条路径现已一致 |
| `quality_issues` 里硬编码 `"blocking": False` | 收购域 8 处、规划域 11 处 | 逐条对照 `is_blocking()`，**全部与分类器判定一致**——写法不规范但结论正确，未压掉任何真阻断码 |

其中 `build_capacity_exceeds_selected_market` 与我判为阻断的 `market_capacity_unit_mismatch` 是同一分支的两个出口，但性质不同：前者是"产能超出市场容量"（业务判断，可能有出口/多市场等合理解释），后者是"量纲不可比"（口径非法）。前者保持非阻断并随件披露是对的——这也验证了阻断判据只针对量纲问题、没有误伤业务判断。

## 六、对象血缘（可追溯）

```
pctx_2a8369e0fc1d639b79066751  ProjectContext rev3（machinery）
├─ iapp_1683e4ae681fd74d1df7f17a   InputApplicability
├─ discovery_c1d00baf815d72a61da2a80e  DiscoverySet（8 候选）
│  └─ src_1ce48fb5d8efa206a0915e83 / src_5dc9c81553f4420636cca3c9  SourceSnapshot
│     └─ analysis_d395bc3f6bae9220e3831b21  AnalysisTask（64,453 字符 / CJK 44,059）
│        └─ cset_c7b64d4133a0e689a8c359ed  CandidateSet（partial）
│           └─ evp_d3731180e740ea3ead955dc0  EvidencePack（partial）
│              └─ drp_84fd9921c95874a4608993d7  ResearchPackage（partial）
├─ mkt_f0ca27225d5e17055c0be340   MarketSizingCase confirmed（9.768 亿元）
├─ scale_e3fab9e0bb2e65653c070c1d BuildScaleCase confirmed
├─ labor_90a11791ffd4c7aaad5cd1e2 LaborPlan confirmed（90 人 / 1050+315 万）
├─ revdrv_954a5d19a29b4c87d3b3ee21 RevenueDriverSet
├─ costdrv_4b26859a17fefa7417285426 CostDriverSet confirmed（49,368 万元）
├─ policy_1db6d09c79272d5b6e3161d7 PolicyBasis confirmed
├─ optcmp_47a3e6e97c0d293b7ef6372f OptionComparison confirmed（opt_a 100 分）
├─ fsp_3c8e28ac06c81fca42c4c176   FinanceSpec confirmed
│  └─ run_7cf8e2bf017d            FinanceRun consistency_ok=true ★干净基准
│     ├─ ftp_bf396b70595eea293a8ce339  FinanceTablesPackage（13 表）
│     ├─ fbs_d76e2589b2149436c9cd4cec  BalanceSheet（12 期全平）
│     ├─ fmc_f131cc1afb5492db4f937d53  MonteCarlo（500 样本）
│     └─ rprep_7a277262421fd6ba554fc609 ReportPreparation
│        └─ rrv_35761b950b5c48176bf9b9d3 ReportRevision
│           ├─ deliverable_36b630ad1e80474f9c59b6ffcd4f1d82  DOCX draft
│           └─ review_9248f81a9b8c8dea709f9dd18625c492  Review（56 blocking findings）
│              └─ rvexp_5468755db993a5f8cb3cabf8  ReviewExport（json+md）
├─ fdr_02eb1c2114844b42a0691550  DeliveryRun（research 已绑定）
└─ zmr_f6fbe4ad18c110e532d279e2  零材料 DeliveryRun（estimate_preview）
```

**产出目录**：`lvke产出/acc20260830/`
- `finance-tables/xlsx/ftp_63b7f16e0e4ba7be3b232b1a.xlsx`（13 sheet，43,382 字节）
- `finance-tables/csv/ftp_63b7f16e0e4ba7be3b232b1a/`（14 个 CSV，全 BOM）
- `report/deliverable_artifacts/artifacts/deliverable_36b630ad1e80474f9c59b6ffcd4f1d82/`（report.docx 236,285 字节 + basis_snapshot.json + manifest.json + index.json）

## 七、受限结论

**首轮（发现阶段）**

> 在本轮实时工具、场景和边界覆盖范围内，发现 2 个阻断性本地 P0 与 7 个 P1，技术金标链未通过验收。

**8-30 轮修复后**

> 在 8-30 轮实时工具、场景和边界覆盖范围内，首轮发现的全部 P0/P1 已修复并复验。全仓回归 629 passed / 0 failed，52 项行为断言全通过。

**8-31 clean build 轮（当前有效结论）**

> 在本轮实时工具、场景和边界覆盖范围内，8-30 轮的 2 P0 + 7 P1 修复全部真实成立且未误伤正常链；但新发现 **4 个 P0 与 7 个 P1**，其中 3 个 P0（内联嵌套 revenue、收购域利润表/资产负债表、零材料行业未解析路径）可产出量级或符号错误的数字并写入交付文件而不阻断。**技术金标链本轮未通过验收。**
>
> 技术金标链、真实资料资格、正式候选和 release 条件分别判定，不相互替代。分级发布门禁（预览/过程/正式三级）与证据分级（real / technical_fixture / controlled_assumption）本轮实测未发现绕过。

三条必须同时成立的边界：

1. **G1+ 资格已取得**（`build_metadata_complete=true`、`build_commit=4c36996`、工作树 clean），但**验收结论为不通过**——门禁资格与验收结果是两件事。
2. **真实资料仍不足**。项目层面数字全部为受控假设/行业基线，江夏区级用地指标与项目原始报价未取得原件。代码修复不改变这一结论。
3. **未触达 ≠ 通过**。8-31 轮已补覆盖 `acquisition_run_model` 完整收购链（12/12 工具）、零材料 10 工具、`lvke-source-files` 13 工具、`lvke-reference` 12 工具、`review_disposition_finding`/`review_export`。**仍未覆盖**：`review_submit_assessment` 七域 Assessment 提交与零材料 `sim_a_formal` 晋升成功路径（两者均为不可伪造 SKIPPED，见上文第三节）；`review_retest` 复测；Git / Playwright 边界用例；DOCX 逐页 PNG 渲染的中文可见性人工核验。

**取证方式**：首轮全部缺陷以真实 MCP 调用取证，代码引用仅用于根因定位。修复阶段的复验采用端到端真实链路（`run_model` → `validate_post_generation` → `assert_publish_finance_binding`）与纯函数断言两种方式，未以单测通过替代行为验证。

---

## 八、8-31 第二轮：六项复核与活体复验（HEAD=462bd9e）

对一份外部提交的 6 条缺陷清单逐条实测判定，**3 条不成立或归因错误**，3 条成立并已修复。判定一律以真实 MCP 调用或可复现探针为准，不采信描述。

### 8.1 判定结果

| 报告项 | 判定 | 依据 |
|---|---|---|
| G1 构建元数据不完整 | **非缺陷** | 仅触发 `tracked_worktree_dirty`（无 `stale_build_commit`）；元数据文件一直有 `build_time`，是解析器主动清空。提交+重新生成+重启后自动恢复 |
| 未确认 fallback FinanceSpec 哈希死锁 | **成立，但描述有误** | 「两个哈希传哪个都不接受」不成立：run 侧自算哈希传回**是接受**的，不传也能跑。真缺陷是 `prepare_spec` 先算哈希后盖章使记录自身不自洽 |
| `review_disposition_finding` schema 为空 | **不存在** | 三法推翻：活体调用带全业务字段得业务错误 `review_not_found` 而非 -32602；属性在 4 个 `oneOf` 分支内；直接 jsonschema 校验 ACCEPTS |
| 审查套件 confirm 哈希不一致 | **成立，归因错误** | 非 `sim_a_formal` 的 run 从不写 `content_hash`，比对空串必然失败；`finance_run_snapshot_hash` 是 `sort_keys` 确定性序列化，不存在"重算漂移"。另发现第二个真根因（见 8.2） |
| DR 质量确认与 submit 不一致 | **成立，方向相反** | 不是 confirm 更严有理，而是 submit 认三类来源、confirm 漏认 `dr_add_sources` 绑定的那类。**未漏调绑定步骤，恰因只走绑定才触发** |
| 旧 CJK 夹具乱码 | **成立，应升级** | 不止预览难看：GB18030「总投资 46968 万元」→ `��Ͷ�� 46968 ��Ԫ`，**数字全存活、标签全毁**，字符数 12→15，locator offset 建在乱码上 |

### 8.2 本轮修复（6 项，均补回归测试）

均为「同一语义两处实现只改一侧」形态，且修复前**全部零测试覆盖**：

1. **纯文本/Markdown 零编码检测** — 文本分支是裸 `decode(utf-8, errors="replace")`，而同一文件 CSV 分支有三级检测。新增 `_decode_text`；不可解字节保留可读部分但附 `text_encoding_undecodable`，不再静默。
2. **DR 双门死锁** — 两道门一起修，并新增固化 `plan_source_hashes`；否则第二道门无 hash 可比，等于为解死锁而放弃篡改检测。进入死锁后无恢复路径（重提报 `task_already_terminal`，`accept_material_limitations` 在引用审计门之后约 50 行才被读，绕不过）。
3. **候选 FinanceSpec 100% 死锁** — 改为先盖章后算哈希，与 `spec_cases.py` 侧顺序一致。**复用已确认 Spec 时不重新盖章**：`object_id` 由 payload 内容派生，重算会打断 `can_reuse_confirmed` 判据。
4. **`run_model` 拿调用方哈希比注入后的 spec** — 改为与注入前身份比对；伪造哈希仍被拒。
5. **两处错误码归因** — 拆出 `formal_finance_run_not_promoted`；`finance_run` 的 `target_sha256` 剔除可追加的 `audit.issues`（原含审查后可写字段，prepare→confirm 之间任何审计写入都会让组件哈希漂移）。
6. **CAGR / `oneOf` / `review_purpose`** — 跨度不足一年拒绝年化（原 2024Q1→Q2 返回 217.6% 且 `issues=[]`）；`transport._validate` 改取**最具体**的校验错误而非第一条，改一处惠及 14 服务全部入口；外部套件 `process_acceptance` 默认值保留但补披露。

### 8.3 活体复验（重启后，`build_commit=462bd9e`）

| 修复 | 活体证据 |
|---|---|
| CJK 编码 | GB18030 md 导入后 `encoding="gb18030"`、`parser="mcp-text-parser.v1"`，正文「总投资 46968 万元」**零替换符** |
| 候选 spec 死锁 | `prepare_spec` → `run_model(spec_id)` → `success=true`、`run_821aa94ba688`、**blockers 为空**、全响应无 `spec_hash_mismatch` |
| 错误码归因 | 同一调用由 `formal_finance_run_content_hash_mismatch` 改报 `formal_finance_run_not_promoted`，并给出两条出路；**实测其建议的 external 路径确实可确认同一 package**（`rvpkg_e72bfe9b`） |
| 判别式可诊断性 | `asset_type="wind"` → `'wind' is not one of ['hotel_lease','solar_power']`；分支内越界 → `spec.solar_operation.installed_capacity_mw: -5 is less than or equal to the minimum of 0` |
| CAGR | 2024Q1→Q2 → `partial` + `cagr_span_below_one_year` + `elapsed_years=0.25`；2022→2024 仍给 20%、`ok` |
| `review_purpose` 披露 | 响应新增「外部套件未显式声明 review_purpose/release_scope，已按 process_acceptance 处理…如需正式口径请显式传入」 |

DR 双门无法单独活体取证（需完整 DR 会话且检索有成本），以回归测试锁定：移除修复后精确复现 `citation_source_not_in_basis:0:plan-source-1`，恢复后通过，且 plan 来源 hash 不符仍报篡改。

### 8.4 门禁与遗留

- **回归 648 passed / 0 failed**（基线 629，新增 19 项为净增而非替换）。`make verify` 的 skills / plugin 两项通过。
- **G1 门禁本轮取得 `build_metadata_complete=true`**（`build_commit=462bd9e` 与 HEAD 一致、无降级原因）。提交顺序教训：元数据必须在**所有提交完成后**生成，且生成后需重启才生效——本轮为此重启两次。
- **仍未修**：十三表缺「财务计划现金流量表」。数据已作为 `FinancialPlan` sheet 输出，`known_gap` 强制该需求永远报 partial（非静默缺口）；补成第 14 张编号表会改变公开的"恰好 13 张"契约，属产品决策。
- 上一轮报告列为"仍未解决"的多数 P2 实际已在当时未提交的工作区中修掉——**读验收文档前须先 `git status`**，报告是快照、工作区是现状。
