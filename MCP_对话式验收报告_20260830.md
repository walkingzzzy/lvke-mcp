# Lvke MCP 当前未关闭问题

| 项 | 值 |
|---|---|
| 对照日期 | 2026-09-02 下午偏晚（**第五轮**重启后对话式活体验收） |
| HEAD / 活体信封 | `build_commit=fe81aa27b720eb527587bf6859c0a9a68bab21e4`（`fix: expose finance reconciliation blockers at MCP boundary`） |
| 构建元数据 | 活体 `complete=false`、`missing=["build_time"]`，按 **dev build** 计，不具 G1+ 正式资格 |
| plugin_version | `0.1.0+codex.20260829115713` |
| 本轮工作区 | `ws20260902r5`（全新；禁止复用 `ws20260902r4` / `ws20260902acc`） |
| 项目 | 湖北江夏智慧农业产业园建设项目（新建 / 农业 / 湖北武汉江夏） |

## 当前状态

- **第四轮仍开的 F-9（P2）本轮活体已关**，不再列为开项。
- **本轮无新的本地 P0/P1/P2 开项。** F-15 保持降级设计；真假数字并存时动态回收期未进 `finance_narrative` mismatch，记为残余观察，不重开 F-2。
- 主链已跑到审查，并拿到过程验收成功终态 **`process_accepted`**（`fdrp_a7ae4dadec22bcd001c3c64f` / `fdr_a0dab243e1f7b02679fea6ac`）。`project_delivery` 继续硬拒，属 EXPECTED_REJECTION。
- 真实资料轨仍为 `partial`：3 份公开政府来源已固化；`project_fact_certified=false`。本轮 EvidencePack `evp_25118c7ef9cbaf4a6fbe2c81` 的 `formal_evidence_candidate=true`，但不能替代正式资格。

> 在本轮实时工具、场景和边界覆盖范围内，未发现阻断性的本地 P0/P1。
> 技术金标链可计算到一致 FinanceRun / 十四表 / 报告修订 / 审查，并完成过程验收。
> **dev build 不能计正式验收。** 真实资料资格、正式候选与 release 条件分别判定，不相互替代。

## 本轮仍开项

无。第四轮唯一开项 F-9 已关。

### 残余观察（不重开已关项）

| 观察 | 不升格原因 | 本轮证据 |
|---|---|---|
| **F-15 降级** | 不一致 run 仍能 `tables_render`，属既有设计，不是门禁绕过 | `run_9b9fb2d50bba` 被标 `blocked` 后仍出 `ftp_3cbd04861ff27a7cfbd3ec95`，`business_success=true` |
| **动态回收期假数字** | 引擎 `dynamic_payback_years=None`，叙事未把 4.2 记入 mismatch；审查侧已抓到，不重开 F-2 | 同段+孤立四假数字：叙事 mismatch 覆盖投资/收入/IRR 各两条；`4.2` 未进叙事 mismatch。审查 `REPORT.NUMBERS.BOUND` 对同段与孤立段的 `dynamic_payback=4.2` 各出 1 条 P0 |
| **过程验收质量提示** | `quality_valid=false` 不阻止 `process_acceptance` | parent binding mismatch（revdrv / fsp / optcmp）、九章未齐、审查 verdict=fail 等进入 `release_limitations`，未进 blockers |

## 本轮已关闭（不要再当缺口）

| 编号 | 结论 | 决定性证据 |
|---|---|---|
| **F-9** | **本轮关闭** | `run_9b9fb2d50bba`：`consistency_ok=false`，`business_success=false`，`status=blocked`，`blocking_issues=[{rule:流动资金分项净额=投资估算流动资金, 847.94 vs 1500, blocking:true}]`；`next_actions` 要求先修复勾稽、不得将当前 run 视为正式候选。`finance_get_run(view=checks)` 顶层 `blockers` 同步带出同一条 |
| **F-1** | 再关，且本轮拿到成功终态 | `process_acceptance` → `status=process_accepted`，`release_id=fdrp_a7ae4dadec22bcd001c3c64f`；`project_delivery` 硬拒 `FORMAL_ARTIFACT_QUALIFICATION_REQUIRED` |
| **F-2** | 已关，未回潮 | 同段+孤立四假数字被叙事与审查抓住；比第四轮更好：同段同时报投资/收入/IRR，不再只报 IRR |
| **F-3** | 已关，未回潮 | `review_f11218e8401caa1e719cfb6fd1bcaa69` 共 **12 个 P0**：8 个 `REPORT.NUMBERS.BOUND`（投资/IRR/收入/动态回收期 × 同段+孤立）+ 4 个 `REPORT.INTERNAL.CONSISTENCY`。`verdict=fail` |
| **D-2** | 已关，未回潮 | draft `deliverable_d5d1049981a348d9ad55fdde9c5c2596` 的 `manifest.blocker_summary` 含 `finance_narrative_mismatch`（6 条）；解 zip 抽 `document.xml`，首页印出同一条阻断，随后印出同段与孤立假数字 |
| **N-1** | 已关 | `feasibility_next_actions` 接受对象、返回动作数组，无 `-32602` |
| **F-4** | 再关 | 附表4 使用计划合计 = 筹措合计 = **25547.94**；资本金 10219.18 + 贷款 15328.76 = 25547.94。附表11 建设期两年贷款提款 7664.38×2、资本金 5109.59×2 |
| **F-5** | 再关 | 规划层 `project_total_investment_wan=26200`（23800+900+1500）；对齐流资后财务 run 总投资 25547.94 |
| **F-7** | 再关 | `revenue_by_year=[4032, 5712, 6720×8]`（单点法×达产率曲线） |
| **F-8** | 再关 | `planning_resolve_industry_skill` → `agriculture` → `lvke-feasibility-study` |

第四轮及更早已关且本轮未回潮：F-6 / F-10 / F-12 / F-13 / F-14 / D-1 / D-5。**F-15** 保持降级设计。C 系列仍关。

## 本轮挡住的攻击（EXPECTED_REJECTION）

| 类别 | 场景 | 结果 |
|---|---|---|
| 正式资格 | `fdr_a0dab243e1f7b02679fea6ac` 走 `project_delivery` | `FORMAL_ARTIFACT_QUALIFICATION_REQUIRED` + `evidence_policy_mismatch`（drp/review/rrv）+ `project_fact_certification_required` |
| 正式资格 | `feasibility_validate(scope=formal)` | `passed=false`，blockers 为证据政策不匹配 |
| 正式资格 | `tables_validate(scope=formal)` | `blocked`（EXPECTED；含量价/产品树/流资语义、BOE 深度） |
| 正式资格 | `report_prepare` 正式 blockers | `finance_tables_package_not_formal` / `project_fact_not_certified` |
| 数字造假 | 同段+孤立 35000 / 18.60% / 12000 / 4.2 | 叙事 mismatch（投资/收入/IRR）+ 审查 8 条 NUMBERS.BOUND P0 + 4 条 INTERNAL.CONSISTENCY |
| SSRF | 云 metadata `169.254.169.254` | `unsafe_public_url` 无条件拒绝 |
| 规划约束 | planB 覆盖率 0.425 | `feasible=false`，`building_coverage_constraint_failed` |
| 输入冲突 | `annual_operating_cost_wan` 与 `cost_items` 同传 | `cost_input_conflict`（沿用既有契约，本轮未重开） |

过程验收本身**放行**：`feasibility_validate(scope=technical)` 对已 `process_accepted` 的 FDR 返回 `passed=true`、`quality_passed=false`，质量问题不阻止后续过程发布。这是过程验收语义，不是 F-1 失效。

## 本轮物理核验（不采信工具自述）

| 对象 | 核验方式 | 结果 |
|---|---|---|
| 附表4 / 附表11 | 读 markdown 原文 | 使用计划合计 = 筹措合计 = 25547.94；建设期贷款提款 7664.38×2、资本金 5109.59×2；建设期融资活动净流入 12773.97×2 = 25547.94 |
| DOCX 字体 | 导出信封 / manifest | `NotoSerifSC-Regular` / `NotoSansSC-Medium`、`missing_cjk_glyph_count=0`、`ofl_license_metadata=true`、`portable_cjk_fonts=true` |
| DOCX 正文 | 解 zip 抽 `document.xml` | 首页印出 `finance_narrative_mismatch`、`finance_reference_structure_incomplete`、`finance_tables_package_invalid`、`owner_missing`；随后印出真值清单与同段/孤立假数字 |
| 引擎算术 | 手工复算 | 规划总投资 23800+900+1500=26200；市场双路径 22400000×0.0003=6720；爬坡 6720×0.6=4032、6720×0.85=5712；资本金/贷款 40/60 = 10219.18 / 15328.76；对齐流资 23800+900+847.94=25547.94 |
| 流资勾稽 | 按引擎 resolution | 分项净额 847.94 vs 申报 1500 时 F-9 进入 `blocking_issues`；对齐后主链 `consistency_ok=true` |

**交付表张数仍是 14 张，不是 13 张。** `tables_list_tables` 列出附表1–11（含 6-1/6-2/6-3），`engine_delivery_count=14`。验收 Skill 正文仍写 13；按实测记。

主链指标（只引用 MCP，不重算 IRR/NPV）：`project_irr_pct=3.04`，`npv_wan=-5050.65`，`static_payback_years=8.24`，`dynamic_payback_years=None`，达产收入 6720，净利润 528.57，BEP 70.77%。

## 两轨结论

| 轨 | 结论 |
|---|---|
| 技术金标 | **第四轮 P2 F-9 已关。** 主链可算、一致 run 可出十四表、同段假数字可被叙事与审查抓住，过程验收拿到 `process_accepted`。无新开项。dev build 不能计 G1+。 |
| 真实资料 | `partial`。3 份政府来源已固化；用地/业主等未决；`project_fact_certified=false`。 |
| 正式候选 | **不可用**。dev build、flat 变体未冻结、量价深度不足、`tables_validate(formal)` 失败、审查 verdict=fail。 |
| release | `process_acceptance` **成功**（`fdrp_a7ae4dadec22bcd001c3c64f`，`quality_valid=false` 写入 limitations）。`project_delivery` 继续硬拒 `FORMAL_ARTIFACT_QUALIFICATION_REQUIRED`。 |

## D 系列

| 编号 | 本轮是否复验 | 结论 |
|---|---|---|
| D-1 | 更早已关，融资提升继续生效 | 关闭 |
| D-2 | **已复验，关闭** | manifest + DOCX 首页均有 `finance_narrative_mismatch`（6 条：投资/收入/IRR 各两条） |
| D-3 | **未复验** | 未进收购域 |
| D-4 | **未复验** | 未进收购域 |
| D-5 | 并入 F-1 | 已关 |

## 本轮未触达（如实，不是 SKIPPED）

可以安全执行但本轮范围未及，不得写成已测：

- 资产收购全链（D-3/D-4）
- 零材料 TemplatePack → 晋升 / 伪造晋升
- DR 恢复链：checkpoint / resume / continue / cancel / 加减来源 / plan revision / bundle
- 审查：retest、disposition、dimension 确认、package confirm、finalize
- 知识治理 snapshot / review / publish
- finance：fact_pack、balance_sheet、promote、package、vendor_review、monte_carlo
- source-files 上传/解析链
- reference archive / geo / template_fill
- Tavily extract / map / crawl / research
- 假阳性三组（披露句 / 敏感性三情景 / 项目 IRR vs 资本金 IRR）本轮正文未写，**不能声称已修**
- 分散四假数字（总投资 / IRR / NPV / 回收期各成段）本轮未单独立修订

本轮补测的只读/纯函数：`finance_calculate`（IRR 纯函数）、`finance_get_run`、`data_provider_status`（Tavily search `probed_ok`，extract 未探）、`planning_get_object`、`planning_get_env_templates`、`lvke_list_resources`、`lvke_read_resource`、`tables_list_tables`、`tables_get_table`、`feasibility_validate`。

`mcp_auth` 不当业务工具。实时 tools/list 分母为 14 个 Lvke + Tavily + cursor；业务工具约 188。上表是明确未触达，不是「其余都过了」。

## 修复建议顺序

1. Skill 正文「13 张表」改为 14
2. 残余：引擎无动态回收期真值时，叙事仍应按 claim 抽出「动态回收期 4.2」假数字（审查侧本轮已覆盖）
3. F-15：不一致 run 出表若要保持降级，须在 `tables_render` 信封明确写「非正式候选 / 不得绑定报告」，不要写「可直接绑定到报告」
4. 假阳性三组与分散四指标补测后再关观察项
5. 过程验收里的 parent binding mismatch（revdrv / fsp / optcmp）建议在规划 confirm 时写回 parent，避免质量提示堆积

## 本轮对象血缘（只使用这些 ID）

工作区 `ws20260902r5`。

```text
pctx_c18c42466c56fd9ed9d4ba6a   ProjectContext（industry_code=农业，basis sha256:fc72ede9…）
└─ iapp_80a7c5a7fbfcfa7f1d49ac99
discovery_f6805d501d5e2db8e86c07c7
├─ src_e7a0d2e2f3d5fb95d0c9bfa2   武汉农农局智慧农业专题
│    analysis hash sha256:d19da8def616d60754301577df3b3c40212a19f81c8d5550187b06fb99b8e7ff
├─ src_db513f0c56d384995cb4cd00   2024 农业农村工作要点
│    analysis hash sha256:f8e18a47bbb64347cfd96c83633aef6fcad02b3a8f669121917e029dce47f7c8
└─ src_0dec2ee7fedd72803e55d0d8   湖北省现代设施农业实施方案（2024-2026）
     analysis hash sha256:f4c1535879084b2ef2e2c7615bc1baaffa73e694f66b8d73735774f428bb3d15
analysis_decdd7d274326a8708f0d074
└─ cset_506e074e4e1ecebaad7cd509
   └─ evp_25118c7ef9cbaf4a6fbe2c81   track=real / formal_evidence_candidate=true
drs_1903359478fd370d595e65dc
└─ drp_21d574adcd9174036fece557 → 质量确认 drp_ce0e31e7da737d7f8f62c787
mkt_f4fce53a76ea1c761442e9dc   双路径 candidate
└─ mkt_7cc8ac0f528c34e2d1b73ad5   confirmed，选 top_down，目标 6720 万元
scale_389001e07225b9fbe9e32d0c → scale_b94daa4277fb7f7cbecb00b1 confirmed planA
optcmp_9f0c31f2a54365babc3b6b57 → optcmp_681e23de93de9217d1beada5 confirmed planA
labor_f7c5282eefdde81968f6ff70 → labor_fe7dd20139d6e33c4fe988f6   40 人，工资 286 + 福利 100.1
revdrv_28864ec48ca3868bd6bac3a8   ramp 生效 [4032, 5712, 6720×8]
costdrv_6b5aee0d38bf64c63fc13051 → costdrv_0dcf6231b6448f6ff4c73bc4 → costdrv_1729a8206efeb0e33a43846d
                                  confirmed 规划总投资 26200 / opex 2660
fsp_c87673a361af47c7613c9aa4 → confirmed fsp_55c59524c361bb8767371920
└─ run_9b9fb2d50bba   流资 1500 vs 分项 847.94（F-9 关闭证据：blocked + blocking_issues）
   └─ ftp_3cbd04861ff27a7cfbd3ec95   不一致 run 仍出包（F-15 降级）
fsp_b9545cdf39d342541078096d → confirmed fsp_8decb7efa05482278349c9c6
└─ run_d594821a4703   consistency_ok=true；IRR 3.04% / NPV -5050.65 / 静回收 8.24
   ├─ fboe_b24537cb7a685ff3b369a88d   BasisOfEstimate formal_ready=true
   └─ ftp_07376fbea5bc7a5d7a89a543   14 表
      └─ XLSX  xlsx-technical
rprep_6109ca750f803b4f09b34573   draft_ready=true / formal_ready=false
└─ rrv_84c9a04ced82b24adfb7eb3c   初始真值快照
   └─ prop_8563a48ce901b03f → rrv_8f7111624521a9153e6d15a8   真值+同段四假数字+孤立四假数字
      ├─ deliverable_d5d1049981a348d9ad55fdde9c5c2596   draft DOCX（D-2 关闭证据）
      └─ review_f11218e8401caa1e719cfb6fd1bcaa69   verdict=fail；12 个 P0
fdr_350817f9ece23d985eb5f611
  → …阶段绑定 fork…
  → fdr_166913616de0acb94d0fc3cb   绑到 review，current_stage=released
     └─ process_acceptance → fdrp_a7ae4dadec22bcd001c3c64f
        └─ fdr_a0dab243e1f7b02679fea6ac   status=process_accepted
           └─ project_delivery 硬拒 FORMAL_ARTIFACT_QUALIFICATION_REQUIRED
```

输出目录：`lvke产出/ws20260902r5/`（`report/deliverable_artifacts/artifacts/deliverable_d5d1049981a348d9ad55fdde9c5c2596`）。

## 方法笔记

本轮只使用实时 MCP，未用 pytest / 历史工作区对象顶替。代码冻结后重启一次，活体信封 `build_commit=fe81aa27`。

F-9 的关闭标准不是「对齐后能算出一致 run」（第四轮已能），而是：**失败勾稽必须进入顶层 `blocking_issues`，且 `business_success=false` / `status=blocked`，不得写「不影响后续生成」。** 本轮 `run_9b9fb2d50bba` 满足该标准。

过程验收成功终态第四轮未拿到，卡在缺 `BasisOfEstimate`。本轮补齐 `fboe_b24537cb7a685ff3b369a88d` 后，`finance_spec` 可绑定，`process_acceptance` 走到 `process_accepted`。
