# Lvke MCP 当前未关闭问题

| 项 | 值 |
|---|---|
| 对照日期 | 2026-09-03（**第七轮**重启后对话式活体验收） |
| HEAD / 活体信封 | `build_commit=44bbd718a080db34b3c33f5d7ae385344c4c7272` |
| 构建元数据 | 活体 `complete=false`、`build_time=build_metadata_incomplete`，缺 `build_time`；工作树含未提交修复，按 **dev / dirty checkout** 计，**不具 G1+ 正式资格** |
| plugin_version | `0.1.0+codex.20260902102519` |
| 本轮工作区 | `ws20260903r7`（全新；禁止复用 `ws20260902r6` / `ws20260902r5` / `ws20260902r4` / `ws20260902acc`） |
| 项目 | 湖北江夏智慧农业产业园建设项目（新建 / 农业 / 湖北武汉江夏） |

## 当前状态

- **第六轮残余观察本轮已复验。** 动态回收期假数字进叙事 mismatch；`finance_run_model` 勾稽失败顶层与 `get_run` 对齐；`feasibility_next_actions` 顶层为字符串数组；规划 confirm 写回 parent；Skill 交付表口径改为 14；F-15 继续降级出表且不得绑定正式报告。
- **本轮无新的本地 P0/P1/P2 开项。** F-9 / F-2 / F-3 / D-2 / F-1 未回潮。
- 主链已跑到审查，并拿到过程验收成功终态 **`process_accepted`**（`fdrp_0b1ff314605fe22108b3bf1c` / `fdr_00faf21b95e44634e9e31134`）。`project_delivery` 继续硬拒，属 EXPECTED_REJECTION。
- 真实资料轨仍为 `partial`：4 份公开政府来源已固化；`project_fact_certified=false`。本轮 EvidencePack `evp_ba107d17b67ee9b173601f64` 的 `formal_evidence_candidate=false` / `project_fact_certified=false`，不能替代正式资格。
- 多数业务调用仍是 `diagnostic_only=true`、`formal_report_allowed=false`。`build_metadata_complete=false` 只过构建门失败，不替代正式资格。

> 在本轮实时工具、场景和边界覆盖范围内，未发现阻断性的本地 P0/P1。
> 技术金标链可计算到一致 FinanceRun / 十四表 / 报告修订 / 审查，并完成过程验收。
> **dev / dirty checkout 不能计正式验收。** 真实资料资格、正式候选与 release 条件分别判定，不相互替代。

## 本轮仍开项

无。第六轮已关项本轮未回潮；第六轮残余观察中应修的项本轮已关。

### 残余观察（不重开已关项）

| 观察 | 不升格原因 | 本轮证据 |
|---|---|---|
| **F-15 降级** | 不一致 run 仍能 `tables_render`，属既有设计，不是门禁绕过 | `run_7b4492b86860` 勾稽失败后仍出 `ftp_5d934d02b86dbd2f7cb6c046`。信封写「不得直接绑定正式报告」，`formal_report_allowed=false`，`quality_status=fail` |
| **过程验收质量提示** | `quality_valid=false` 不阻止 `process_acceptance` | 九章未齐、审查 verdict=incomplete、`project_fact_not_certified` 等进入 `release_limitations`，未进 blockers。**本轮 limitations 不再含 parent binding mismatch** |
| **正式资格** | 本轮未做 EVD-2 晋升；属 EXPECTED | `feasibility_validate(scope=formal)` 与 `project_delivery` 硬拒 `FORMAL_ARTIFACT_QUALIFICATION_REQUIRED` + `evidence_policy_mismatch` |

## 本轮已关闭（不要再当缺口）

| 编号 | 结论 | 决定性证据 |
|---|---|---|
| **动态回收期假数字** | **本轮关闭** | `report_validate(rrv_1b9d78289b81c6f3b4f3147d)`：`finance_narrative.mismatches` 含两条 `dynamic_payback`，`expected=null`，`found=4.2`，`reason=run_missing_expected_value`。draft manifest `mismatch_count=8`（投资/收入/IRR/动态回收期各两条） |
| **run_model 信封** | **本轮关闭** | `run_7b4492b86860`：`business_success=false` / `status=blocked`，顶层 `blockers=["流动资金分项净额=投资估算流动资金"]`。`finance_get_run(view=checks)` 仍 blocked。F-9 未回潮 |
| **next_actions schema** | **本轮关闭** | `feasibility_next_actions` 顶层为 string[]；可执行描述在 `actions`（含 tool/arguments/reason），不再被 MCP 输出 schema `-32602` |
| **parent binding** | **本轮关闭** | `scale_45c92f8db64bbdc84b6d2ba0` parent 含 `optcmp_7a0d5262da292c1225825399`；`revdrv_7a3abc403873e6f0c0b3b3b9` parent 含 `scale_45c92f8db64bbdc84b6d2ba0`。过程验收 `release_limitations` **无** `*_object_parent_binding_mismatch` |
| **Skill「13 张表」** | **本轮关闭** | 验收 Skill 正文已改为按 `engine_delivery_count` / `tables_list_tables` 记 14。活体 `ftp_fc4fe3d80b7a014f7833efb5`：`engine_delivery_count=14`；`report_prepare` 合同 `engine_delivery_count=14` / `reference_source_sheet_count=15` |
| **F-9** | **再关，未回潮** | 不一致 run：申报流资 1500 vs 周转净额 **849.56**，进入 `blocking_issues`（`blocking:true`）。查询侧 `business_success=false` / `status=blocked`。`next_actions` 要求先修勾稽、不得当正式候选 |
| **F-1** | 再关，且本轮拿到成功终态 | `process_acceptance` → `status=process_accepted`，`release_id=fdrp_0b1ff314605fe22108b3bf1c`；`project_delivery` 硬拒 `FORMAL_ARTIFACT_QUALIFICATION_REQUIRED` |
| **F-2** | 已关，未回潮；本轮补上动态回收期 | 同段+孤立四假数字被叙事与审查抓住；叙事同时报投资/收入/IRR/**动态回收期** |
| **F-3** | 已关，未回潮 | `review_ef2a760225daa7a9a1cb64ee5491f1d3` 共 **10 个 P0**：8 个 `REPORT.NUMBERS.BOUND`（含 `dynamic_payback=4.2`）+ 2 个 `REPORT.INTERNAL.CONSISTENCY`。`overall_verdict=incomplete` |
| **D-2** | 已关，未回潮 | draft `deliverable_b91949bc93f140b2b925ebb4028aa601` 的 `manifest.blocker_summary` 含 `finance_narrative_mismatch`（8 条，含动态回收期）；解 zip 抽 `document.xml`，首页印出同一组阻断，随后印出真值清单与同段/孤立假数字（含「动态回收期4.2年」） |
| **N-1** | 再关 | 阶段绑定接受对象；`feasibility_next_actions` 输入与输出均不再因对象/schema 被 `-32602` |
| **F-4** | 再关 | 附表4 使用计划合计 = 筹措合计 = **25549.56**；资本金 10219.82 + 贷款 15329.74 = 25549.56。附表11 建设期两年贷款提款 7664.87×2、资本金 5109.91×2 |
| **F-5** | 再关 | 规划层 `project_total_investment_wan=26200`（23800+900+1500）；对齐流资后财务 run 总投资 **25549.56** |
| **F-7** | 再关 | `revenue_by_year=[4032, 5712, 6720×6]`（单点法×达产率曲线；本轮运营期 8 年） |
| **F-8** | 再关 | `planning_resolve_industry_skill` → `agriculture` → `lvke-feasibility-study` |

第四轮及更早已关且本轮未回潮：F-6 / F-10 / F-12 / F-13 / F-14 / D-1 / D-5。**F-15** 保持降级设计，误导信封未回潮。C 系列仍关。

## 本轮挡住的攻击（EXPECTED_REJECTION）

| 类别 | 场景 | 结果 |
|---|---|---|
| 正式资格 | `fdr_f4ca501eb73a856670561b4d` 走 `project_delivery` | `FORMAL_ARTIFACT_QUALIFICATION_REQUIRED` + `evidence_policy_mismatch`（drp/evp/fsp/ftp/review/rrv/run）+ `project_fact_certification_required` |
| 正式资格 | `feasibility_validate(scope=formal)` | `passed=false`，blockers 为证据政策不匹配 |
| 正式资格 | `report_prepare` 正式 blockers | `finance_tables_package_not_formal` / `project_fact_not_certified` |
| 数字造假 | 同段+孤立 35000 / 18.60% / 12000 / 4.2 | 叙事 mismatch 8 条（投资/收入/IRR/动态回收期各两条）+ 审查 8 条 NUMBERS.BOUND P0 + 2 条 INTERNAL.CONSISTENCY |
| 规划约束 | planB 覆盖率 0.425 | `feasible=false`，`coverage_cap` / `building_coverage_constraint_failed` |

过程验收本身**放行**：`feasibility_validate(scope=technical)` 返回 `passed=true`、`quality_passed=false`，质量问题不阻止后续过程发布。这是过程验收语义，不是 F-1 失效。

## 本轮物理核验（不采信工具自述）

| 对象 | 核验方式 | 结果 |
|---|---|---|
| 附表4 / 附表11 | `tables_get_table(..., format=markdown)` 读原文 | 使用计划合计 = 筹措合计 = 25549.56；建设投资 11900×2、利息 450×2、流资 424.78×2；资本金 5109.91×2=10219.82、贷款 7664.87×2=15329.74。附表11 建设期融资活动净流入 12774.78×2 = 25549.56 |
| DOCX 字体 | 导出信封 | `NotoSerifSC-Regular` / `NotoSansSC-Medium`、`missing_cjk_glyph_count=0`、`ofl_license_metadata=true`、`portable_cjk_fonts=true`；zip 内含 `word/fonts/lvke-body-cjk.odttf` 与 `lvke-heading-cjk.odttf` |
| DOCX 正文 | 解 zip 抽 `document.xml` | 首页印出 `FORMAL_ARTIFACT_QUALIFICATION_REQUIRED`、`finance_narrative_mismatch`、`finance_reference_structure_incomplete`、`finance_tables_package_invalid`、`owner_missing`；随后印出真值清单（25549.56 / 6720 / 5.2% / -3072.39 / 6.6）与同段/孤立假数字（35000 / 12000 / 18.60% / 动态回收期4.2年） |
| 引擎算术 | 手工复算 | 规划总投资 23800+900+1500=26200；爬坡 6720×0.6=4032、6720×0.85=5712；资本金/贷款 40/60 = 10219.82 / 15329.74；对齐流资 23800+900+849.56=25549.56 |
| 流资勾稽 | 按引擎 resolution | 分项净额 849.56 vs 申报 1500 时 F-9 进入 `blocking_issues`；对齐后主链 `consistency_ok=true`。不显式传 `wc_turnover` 时引擎会走 `ratio_backsolve` 自洽，**测 F-9 必须在 `finance_run_model` 的 `input_revision` 里带周转天数对象** |
| 叙事 mismatch | `report_validate` + manifest | 8 条 mismatch；`dynamic_payback` 两条 `expected=null` / `reason=run_missing_expected_value`；`unmapped=[]` |

**交付表张数仍是 14 张，不是 13 张。** `tables_list_tables` 列出附表1–11（含 6-1/6-2/6-3），`engine_delivery_count=14`。

主链指标（只引用 MCP，不重算 IRR/NPV）：`project_irr_pct=5.2`，`npv_wan=-3072.39`，`static_payback_years=6.6`，`dynamic_payback_years=None`，达产收入 6720，净利润 1533.59。

收入口径：本轮必须用 `price_unit=yuan`、`price_per_unit=1`、`capacity=6720`（数值上等于万元当量）。`price_unit=wan` 会把 expand 的「元」当「万元」，IRR 飙到数千百分比；早期失败 run `run_1bab7f3f96c4` 不得当主链。

## 两轨结论

| 轨 | 结论 |
|---|---|
| 技术金标 | **第六轮残余已关。** 主链可算、一致 run 可出十四表、同段+孤立假数字（含动态回收期）可被叙事与审查抓住，过程验收拿到 `process_accepted`。无新开项。dev / dirty checkout 不能计 G1+。 |
| 真实资料 | `partial`。4 份政府来源已固化；用地/业主等未决；`project_fact_certified=false`；EvidencePack `formal_evidence_candidate=false`。 |
| 正式候选 | **不可用**。dev build、dirty checkout、量价深度不足、审查 verdict=incomplete、`formal_report_allowed=false`。 |
| release | `process_acceptance` **成功**（`fdrp_0b1ff314605fe22108b3bf1c`，`quality_valid=false` 写入 limitations，**无 parent binding mismatch**）。`project_delivery` 继续硬拒 `FORMAL_ARTIFACT_QUALIFICATION_REQUIRED`。 |

## D 系列

| 编号 | 本轮是否复验 | 结论 |
|---|---|---|
| D-1 | 更早已关，融资提升继续生效 | 关闭 |
| D-2 | **已复验，关闭** | manifest + DOCX 首页均有 `finance_narrative_mismatch`（8 条：投资/收入/IRR/动态回收期各两条） |
| D-3 | **未复验** | 未进收购域 |
| D-4 | **未复验** | 未进收购域 |
| D-5 | 并入 F-1 | 已关 |

## 本轮未触达（如实，不是 SKIPPED）

可以安全执行但本轮范围未及，不得写成已测：

- 资产收购全链（D-3/D-4）
- 零材料 TemplatePack → 晋升 / 伪造晋升
- DR 恢复链：checkpoint / resume / continue / cancel / 加减来源 / plan revision / bundle
- 审查：retest、disposition、dimension 确认、package confirm、finalize
- 知识治理 snapshot / review / publish
- finance：fact_pack、balance_sheet、promote、package、vendor_review、monte_carlo
- source-files 上传/解析链
- reference archive / geo / template_fill
- Tavily extract / map / crawl / research
- 假阳性三组（披露句 / 敏感性三情景 / 项目 IRR vs 资本金 IRR）本轮正文未写，**不能声称已修**
- 分散四假数字（总投资 / IRR / NPV / 回收期各成段）本轮未单独立修订
- SSRF / 云 metadata 本轮未复打（第六轮已拒，本轮未重复）

本轮补测的只读/纯函数：`finance_get_run`、`data_provider_status`（Tavily search `probed_ok`）、`planning_resolve_industry_skill`、`tables_list_tables`、`tables_get_table`、`feasibility_validate`、`feasibility_next_actions`、`report_validate`、`review_list_findings(severity=P0)`。

`mcp_auth` 不当业务工具。实时 tools/list 分母为 14 个 Lvke + Tavily + cursor。上表是明确未触达，不是「其余都过了」。

## 修复建议顺序

1. 冻结并提交本轮已生效的 dirty 修复，补齐 `build_time` 后再做一轮干净重启；否则不得把本轮计为 G1+
2. F-15：降级出表可保留，继续保持「非正式候选 / 不得绑定报告」；不要回到「可直接绑定到报告」
3. 假阳性三组与分散四指标补测后再关观察项
4. 正式候选仍缺 EVD-2 / 量价深度 / 九章结构 / 审查关闭；不要用过程验收成功替代

## 本轮对象血缘（只使用这些 ID）

工作区 `ws20260903r7`。

```text
pctx_42484b2340f35de6a4da7ab9   ProjectContext（industry_code=农业，basis sha256:e5a68b5ebe9cf1dba44da7912e196bf8eaaff7d62ab1074cbdfc6e1bc08ee42f）
└─ iapp_6d5f35a47cb60b0b97fe59d0
discovery_99bd2fceaefa3dd88f04f9fd
collection_e49b43fe87be5d51afab2d1c
├─ src_e2e4cb5d687376cb64fe9add   省设施农业实施方案 2024-2026
│    正文 sha256:1dcde3791a6b50f5a4d038a29181b6c79fdae2a810353d299a51e04b5940a052
├─ src_eca3f0d1f521307de6556bb7   2025 智慧农业典型案例
├─ src_382213b8608237786e75e51c   2024 农业农村工作要点
└─ src_cd360ae0a1323590b8332b3e   省委省政府乡村振兴实施意见
     正文 sha256:9b9e9746667e1066a06eb881fc701a58f8557951113c9a6caa6310c5ab9c902c
analysis_1d2d7f56263663244c4245c1
└─ cset_c259e32ec99b091f6eccab93
   └─ evp_ba107d17b67ee9b173601f64   track=real / formal_evidence_candidate=false
      └─ qd_ea791eb4bb5181213075cf7e
drs_15ffc264696addf0a5b7bcc6
└─ drp_5d1a2719a116609556a951ba → 质量确认
   └─ drp_280641592bf69425072e1793   accepted_with_limitations（本轮必须用这个新 ID）
mkt_0aba508147cc75aa1626e122
└─ mkt_3e47ee3152689455a6a15f44   confirmed，选 top_down，目标 6720 万元
optcmp_7831051cd90e4f6e1965fc79
└─ optcmp_7a0d5262da292c1225825399   confirmed planA
scale_97573b4a99c7ae23bfe40c6d
└─ scale_45c92f8db64bbdc84b6d2ba0   confirmed planA；parent 含 optcmp_7a0d5262…
revdrv_484bdf5db0974a4cc8f4887a
└─ revdrv_7a3abc403873e6f0c0b3b3b9   confirmed ramp；parent 含 scale_45c92f8…
costdrv_bda1282d7b8916d4677a0ee3
└─ costdrv_d2fb85603031bf7c3406971f   规划总投资 26200 / opex 2660
labor_98b2fb2b5792c55315d79397
└─ labor_3ff8c335138c34286435122c   40 人，工资 304.8 + 福利 106.68
fsp_449096474f6768a04c102029 → confirmed fsp_8992ddae71e64c3983b1e465
└─ run_7b4492b86860   流资 1500 vs 分项 849.56（F-9 + run_model 信封关闭证据）
   └─ ftp_5d934d02b86dbd2f7cb6c046   不一致 run 仍出包（F-15 降级；不得绑定正式报告）
fsp_132bf6c60548d5662a15f64d → confirmed fsp_5e239602b0ba309af171c27c
└─ run_aecbde2336eb   consistency_ok=true；IRR 5.2% / NPV -3072.39 / 静回收 6.6 / 动态回收期 None
   ├─ fboe_4a95888a65d085632ed1cd33   BasisOfEstimate technical_ready=true / formal_ready=true
   └─ ftp_fc4fe3d80b7a014f7833efb5   14 表
rprep_313e329d9fc3143aa0e2281b   draft_ready=true / formal_ready=false
└─ rrv_0443018c614b612a89656c79   初始真值快照
   └─ prop_a4a12d3dc4fa505e → rrv_1b9d78289b81c6f3b4f3147d   真值+同段四假数字+孤立四假数字
      ├─ deliverable_b91949bc93f140b2b925ebb4028aa601   draft DOCX（D-2 + 动态回收期关闭证据）
      └─ review_ef2a760225daa7a9a1cb64ee5491f1d3   verdict=incomplete；10 个 P0
fdr_abcc0316a9c5c5552d922921
  → …阶段绑定 fork…
  → fdr_f4ca501eb73a856670561b4d   绑到 review，current_stage=released
     └─ process_acceptance → fdrp_0b1ff314605fe22108b3bf1c
        └─ fdr_00faf21b95e44634e9e31134   status=process_accepted
           └─ project_delivery 硬拒 FORMAL_ARTIFACT_QUALIFICATION_REQUIRED

废弃/失败（不得当主链）：
run_1bab7f3f96c4   price_unit=wan 万元放大
run_dd509f4118ce   未传 wc_turnover，ratio_backsolve 自洽
```

固化 EvidencePack locator（市场/方案证据必须用**正文 hash + 完整 locator 对象**，不是 snapshot hash）：

- src_e2e4cb：`content_hash=sha256:1dcde379…`，`url=https://nyt.hubei.gov.cn/zfxxgk/zc_GK2020/gfxwj_GK2020/202502/t20250210_5536554.shtml`
- src_cd360a：`content_hash=sha256:9b9e9746…`，`url=https://www.wuhan.gov.cn/ztzl/qjzx/202603/t20260316_2740333.shtml`

输出目录：`lvke产出/ws20260903r7/`（`report/deliverable_artifacts/artifacts/deliverable_b91949bc93f140b2b925ebb4028aa601`）。

## 方法笔记

本轮只使用实时 MCP，未用 pytest / 历史工作区对象顶替。用户重启后活体信封 `build_commit=44bbd718`，但工作树 dirty，未提交修复经 editable install 生效。本轮只复验活体行为，不以代码扫描顶替。

F-9 的关闭标准不是「对齐后能算出一致 run」，而是：**失败勾稽必须进入 `blocking_issues`，且查询侧与 `run_model` 顶层均为 `business_success=false` / `status=blocked`，不得写「不影响后续生成」。** 本轮 `run_7b4492b86860` + `finance_get_run(view=checks)` 满足该标准。

动态回收期关闭标准：引擎真值为 `None` 时，正文「动态回收期 4.2」必须进 `finance_narrative.mismatches`，`expected=null`，`reason=run_missing_expected_value`。不得只靠审查侧兜底。

过程验收成功终态继续依赖 `BasisOfEstimate`。本轮补齐 `fboe_4a95888a65d085632ed1cd33` 后，`finance_spec` 可绑定，`process_acceptance` 走到 `process_accepted`。BoE 的 `evidence_eligibility` 必须与源对象 `evidence_track=real` 对齐，用 **`formal_evidence`**，不要 `source_reconstructed`。

不要用第六轮数字当本轮真值。第六轮对齐流资是 1053.30 / IRR 2.91% / 总投资 25753.30；本轮是 **849.56 / 5.2% / 25549.56**，因为 opex/折旧基数不同。
