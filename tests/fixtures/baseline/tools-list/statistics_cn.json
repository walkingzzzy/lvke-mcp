[
  {
    "name": "query_indicator",
    "description": "按指标名称(如 GDP / POPULATION / CPI)+ 地区 + 年份查询统计指标序列。",
    "inputSchema": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },
        "region": {
          "type": "string"
        },
        "year": {
          "type": "integer"
        }
      },
      "required": [
        "name"
      ]
    }
  },
  {
    "name": "list_dictionaries",
    "description": "列出本地数据库可用的指标 / 地区 / 年份字典。",
    "inputSchema": {
      "type": "object",
      "properties": {}
    }
  }
]
